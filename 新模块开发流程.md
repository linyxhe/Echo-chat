# Echo Chat Room 新模块开发流程

> 本文档依据当前实际代码制定：前端为 Vue 3 + Vite，业务页面主要位于 `echo-frontend/src/views/`；后端为 Spring Boot + MyBatis-Plus，业务代码位于 `echo-backend/src/main/java/com/echo/`；MySQL 建表脚本为 `echo-backend/src/main/resources/echo_chat.sql`。每个阶段通过验收后才能进入下一阶段。

## 开发总流程

```text
阶段范围确认
  → 数据模型与 API/事件契约
  → 后端实现与单元测试
  → 前端页面/状态/错误处理
  → 前后端联调与异常场景验证
  → 文档、配置与开发日志更新
  → 阶段验收
```

每个功能都必须沿用此流程。接口、WebSocket 事件或数据表发生变化时，应在实现前先更新接口契约和迁移脚本；完成后记录实际改动、验证方式和未解决风险。

## 阶段 0：工程基线与安全配置

**目标：** 让后续模块可以安全配置、可重复建库和可验证地开发。

**工作项：**

- 梳理并移除代码库中的明文密码、JWT 密钥、邮箱授权码和未来的 OSS/模型凭证；不提交的本地配置提供。
- 将后端从 Spring Boot 3.0.2 升级至 **Spring Boot 3.5.x 的当前稳定补丁版本**，保留 Java 17；先处理 Spring Security、MyBatis-Plus、Druid、Redis 与邮件依赖的兼容性，再引入 LangChain4j。
- 将数据库结构调整为“基线 SQL + 增量迁移”的管理方式，禁止新功能直接手改已有表而不留下迁移记录。
- 当前仅建立本地 `dev` 配置与示例配置；不接入阿里云、OSS 或云端部署。本地磁盘存储作为当前唯一的文件存储实现，生产环境配置留待未来单独规划。
- 规范后端模块边界：控制器 → 服务 → 数据访问；新增功能不得继续把跨模块逻辑堆进 `ChatView.vue` 或 `ChatEndpoint`。
- 明确构建与验证命令：后端 Maven 测试、前端 Vite 构建，以及关键接口的联调清单。

**涉及位置：** `application.yml`、安全配置、数据库脚本、`pom.xml`、前端 `config.js` / `src/util/`。

**验收：** 使用无敏感信息的示例配置可启动开发环境；新环境可从基线与迁移脚本完成建库；Spring Boot 3.5.x 后端测试与前端构建成功。

## 阶段 1：重点移动端前端优化

**前置条件：** 阶段 0 完成。

**目标：** 将当前零散的页面级响应式代码升级为统一的移动端体验，优先保证聊天、联系人和动态等高频功能可在手机浏览器中稳定使用。

**工作项：**

- 建立统一的移动端断点、间距、触控尺寸和安全区规则；将分散在页面内的 `matchMedia` 判断收敛为可复用的视口组合式工具。
- 优化 `HomeView.vue`：在小屏幕使用适合单手操作的导航方式，避免侧栏、标题和内容区域互相挤压；路由切换后保持正确的滚动与选中状态。
- 优化 `ChatView.vue`：完善会话列表/聊天详情的单栏切换、返回行为、输入框与软键盘遮挡处理、消息长文本与文件卡片换行、全屏弹窗和触摸目标尺寸。
- 适配 `FriendView.vue`、`PostView.vue`、`SettingsView.vue` 与登录/注册页：表单改为单列、弹窗宽度适配、图片网格与操作栏不溢出、上传与提交操作易于触达。
- 为管理端页面至少提供可读的窄屏降级布局；复杂表格采用横向滚动、卡片摘要或明确提示，不强行压缩桌面表格。
- 保持桌面端布局和 Electron 打包行为不回归；本阶段不修改已完成的音视频通话生产化实现。

**涉及位置：** `src/css/main.css`、新增或调整 `src/composables/`、`HomeView.vue`、`ChatView.vue`、`FriendView.vue`、`PostView.vue`、`SettingsView.vue`、登录/注册页与管理端页面。

**验收：** 在 360px、390px、414px 和 768px 宽度下，无横向页面溢出；核心按钮触控区域充足；聊天输入不被软键盘遮挡；登录、聊天、加好友、发动态、修改资料等核心流程可完整完成；桌面端构建与主要布局无回归。

## 阶段 2：文件服务抽象与大文件断点续传

**前置条件：** 阶段 0 完成。

**目标：** 保留 `/chat/file/upload` 作为小文件兼容接口，并以 Tus 开源协议提供可恢复的大文件上传。

**工作项：**

- 部署开源 `tusd` 服务作为 Tus 上传端点；Spring Boot 不接收大文件字节流，只负责鉴权、业务规则、上传意图与文件元数据。
- 后端新增文件领域模块（文件元数据、上传意图、存储服务接口）和迁移；创建上传意图时校验用户、文件用途、接收方、类型、大小和配额。
- 前端使用 `@uppy/core + @uppy/tus`，复用 Element Plus 的上传界面；通过 Tus 的 `POST` 创建、`HEAD` 查询偏移和 `PATCH` 续传实现断网/刷新恢复，首版不启用并行拼接扩展。
- Tus 完成事件回调 Spring Boot，或由前端调用完成确认接口；后端二次校验上传归属、大小、哈希与扫描结果，状态变为 `READY` 后才能创建文件消息。
- 当前使用本机 `tusd` 与本地磁盘完成协议联调；阿里云 OSS 或其他对象存储不属于本期范围，未来如需部署再单独评估替换。即使本地运行，也应通过受控下载接口访问文件，不能继续默认公开整个上传目录。
- 增加上传任务过期、取消、临时文件和无引用文件的清理策略及测试。

**涉及位置：** 新增 `tusd` 部署配置、`ChatController`、`ChatServiceImpl`、`WebMvcConfig`、新增 file 包、`echo_chat.sql`/迁移目录、`src/util/`、三个上传页面。

**验收：** 小文件上传不回归；中途刷新、断网重连后从 Tus 偏移量继续上传；重复完成不会产生重复文件或重复消息；未完成/未扫描文件不能发送；无权限用户无法直接读取受保护文件。

## 阶段 3：消息可靠性、离线同步与多节点准备

**前置条件：** 阶段 0 完成；阶段 2 不阻塞本阶段，但文件消息必须复用阶段 2 的文件元数据。

**目标：** 将当前单机内存在线表和仅有 `MESSAGE_ACK` 的机制升级为可恢复、可去重的消息链路。

**工作项：**

- 为消息补充客户端幂等 ID、发送/送达/已读状态及必要索引；设计离线同步游标，避免重复拉取。
- 定义 WebSocket 契约：连接后的未读同步、确认送达、已读回执、重连补偿和错误码；更新 `api文档.md`。
- 将 `ChatEndpoint` 中的单机 `ConcurrentHashMap` 在线状态抽象为 Redis 状态；多实例消息转发使用 Redis Pub/Sub 或消息队列，并以 MySQL 持久化消息为最终事实来源。
- 前端将临时消息状态从“已发送/失败”扩展为发送中、已发送、已送达、已读、失败；重连后按幂等 ID 对账并补发失败消息。
- 覆盖重复投递、服务重启、用户离线、网络切换和两端同时登录场景。

**涉及位置：** `ChatEndpoint`、消息/会话实体与 Mapper、Redis 配置、`src/util/webSocket.js`、`ChatView.vue`。

**验收：** 同一客户端消息多次提交只落库一次；离线消息可在登录或重连后补齐；单实例重启不丢失已确认消息；两实例部署时可跨实例实时收消息。

## 阶段 4：AI Gateway 与流式机器人聊天

**前置条件：** 阶段 0 和阶段 3 完成；模型供应商、模型名称、预算和内容安全规则已确定。

**目标：** 以独立 AI 后端模块和 LangChain4j 接入流式模型能力，而不是在前端直接调用模型服务。

**工作项：**

- 新增 AI 领域模型与迁移：机器人、AI 会话、AI 消息、用量日志；模型密钥仅保存在后端环境配置。
- 使用 LangChain4j 的模型接口和 Spring Boot 集成封装模型适配层；按机器人配置选择第三方模型供应商，支持后续替换或新增 OpenAI 兼容及其他 API。
- 建立上下文摘要/截断策略、超时/重试/降级策略和敏感内容处理入口，业务层不得直接依赖某一家模型 SDK。
- 定义流式 API（优先 SSE）：创建/继续会话、取消生成、查询历史；事件包括增量文本、完成、错误和用量。
- 新建 AI 聊天页面或独立组件与路由，复用现有登录态、消息展示和错误提示；不直接改造普通好友聊天协议。
- 记录首 Token 时间、总响应时间、失败率和 Token 用量，并为异常响应提供可恢复提示。
- 暂且使用deepseek的deepseek-v4-flash模型

**涉及位置：** 新增 `ai` 后端包、数据库迁移、前端 `src/views/` 与 `src/router/index.js`、请求工具。

**验收：** 前端不含模型密钥；可持续接收并取消流式输出；重进页面能读取历史；超时或模型失败有明确反馈且不影响普通聊天。

## 阶段 5：知识库、角色与互动玩法

**前置条件：** 阶段 4 完成；已选定向量存储方案和文档来源范围。

**目标：** 在 AI Gateway 上叠加可控的检索、角色和小游戏能力。

**工作项：**

- 知识库文档上传、解析、切片、向量化、检索和权限控制；仅将检索到的相关片段送入模型。
- 实现机器人角色配置，并以服务端模板管理系统提示词和输出约束。
- 将真心话大冒险、二十问、词语接龙、情景推理等做成“游戏状态机 + AI 生成”的独立领域，不与普通 AI 会话状态混用。
- 增加文档删除后的向量清理、检索命中记录、输入输出审核与用量限额。

**验收：** 不同用户或机器人之间的知识库严格隔离；删除文档后不会继续被检索；游戏可恢复状态并能处理非法步骤。

## 阶段 6：社交增强与治理能力

**前置条件：** 阶段 3 完成；群聊的产品规则、权限模型与审核规则已确认。

**目标：** 在稳定消息底座上扩展群聊、搜索、通知、语音消息、拉黑和内容治理。

**建议顺序：** 群聊数据模型与权限 → 群消息链路 → 搜索与未读通知 → 语音消息 → 拉黑/举报/审核联动。

**验收：** 每项能力均复用阶段 3 的幂等、同步、送达和权限校验；管理后台能够审计关键操作。

## 阶段 7：发布、监控与回归

**目标：** 在每个可发布阶段执行，而非留到全部功能完成后一次性处理。

**工作项：** 配置检查、数据库备份与回滚方案、健康检查、结构化日志、关键指标、异常告警、前后端构建、关键流程回归和部署说明。

**验收：** 可从空环境部署；能回滚应用与迁移；可以定位上传、消息、AI 与移动端关键流程的失败原因。

## 当前首个实施切片

第一期不同时启动所有基础设施，而是按以下最小闭环推进：

1. 完成阶段 0 的敏感配置、迁移机制和构建基线。
2. 实现阶段 1 的移动端高频流程适配，优先主框架、聊天、好友、动态和设置。
3. 对移动端核心流程验收后，开始阶段 2 的“本地存储版分片上传”：初始化、分片、查询、完成、取消及前端恢复。
4. 对上传闭环验收后，直接开始阶段 3 的消息可靠性改造；云存储接入留待未来部署阶段单独规划。

这样可以先验证上传协议和前端恢复逻辑，避免在账号、网络或云服务配置尚未确定时被外部依赖阻塞。
