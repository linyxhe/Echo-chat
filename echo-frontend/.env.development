# 开发环境配置（仅 npm run dev 时生效，生产构建不加载此文件）
# 让前端在开发时直连本机后端，不经过云服务器中转。
# request.js 中 import.meta.env.VITE_API_BASE 的优先级高于 public/config.js 的运行时配置。
VITE_API_BASE=http://localhost:8088
VITE_WS_HOST=localhost:8088
VITE_WS_PROTOCOL=ws
VITE_API_BASE=http://localhost:8088
