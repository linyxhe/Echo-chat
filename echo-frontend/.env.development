# 开发环境配置（仅 npm run dev 时生效，生产构建不加载此文件）
# 开发环境统一走 Vite 代理（HTTP 相对 /api、WS 同 host /ws）：
# getApiOrigin / buildWebSocketUrl 在 import.meta.env.DEV 时短路为相对路径，
# 本机与局域网设备（手机连热点访问 172.25.x.x:8089）都经代理指向本地后端 8088。
# 不要在此设置 VITE_API_BASE / VITE_WS_HOST 为 localhost——那会让手机直连自身 localhost 失败。
