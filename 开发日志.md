- 

# Echo Chat Room 开发日志

> **使用约定**：每次开始开发前，先阅读本文档的“最近一次开发记录”和“下一步计划”；完成开发并完成验证后，在“开发记录”顶部新增一条记录。未验证的内容不能标记为完成。

## 当前进度概览

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| 本地开发基础设施 | 已完成 | Spring Boot 3.5.14、私有本地配置、Flyway 基线迁移、真实本机启动与构建验证已完成 |
| 大文件断点续传 | 进行中 | 已完成 tusd 本机服务、文件元数据、受控下载、Uppy Tus、小文件受控迁移与关闭旧公开目录（阶段 C）；待真机断网回归 |
| 消息可靠性与多节点 | 进行中 | v1/v2/v3 完成：在线状态/心跳淘汰/已读展示/重连补拉、多标签页会话路由 + read_at、**跨节点实时推送（Redis pub/sub）+ 隐私开关（在线/已读默认隐藏）**；待浏览器真机验收 |
| 移动端前端优化 | 进行中 | 已完成主框架、聊天、好友、动态、设置与认证页的基础适配；待真机回归与管理端窄屏降级 |
| 音视频通话生产化 | 已完成 | 已完成，不纳入后续优化范围 |
| AI Gateway 与流式聊天 | 进行中 | DeepSeek 流式对话、多轮记忆、Markdown、清空/开关、自定义 AI 助手和停止生成均已完成；消息列表已支持 AI/备注/会话操作，待真实账号回归和用量统计 |
| 知识库与对话游戏 | 进行中 | RAG 已支持相关性阈值、来源元数据、多分类范围、文档 AI 可用开关和用户私有助手资料；待真实权限回归、向量库升级、扫描件 OCR、共享知识库和对话游戏状态机 |
| 社交增强功能 | v3 验收完成 | v1 群聊/通知、v2 全文搜索、v3 群文件/群邀请/语音/内容审核均已完成并完成浏览器回归；群内已读回执已撤回 |

## 下一步计划

1. 浏览器真机验收本轮 AI 流式停止生成、RAG 来源展示及自定义助手隔离链路。
2. 设计并落地对话游戏第一阶段：游戏模板、会话状态、回合推进、结束条件和断线恢复。
3. 知识库增强：扫描件 OCR、向量库升级（pgvector/ES/milvus，数千分片以上）、用户助手可见范围的文档权限。
4. AI Gateway 增强：模型/供应商适配层、超时重试与降级、token 用量和调用审计；大模型支持硬取消时再接入真正的远端取消。
5. 消息可靠性 v4：跨节点已就绪（pub/sub），可选升级——订阅并发消费/分片（当前单线程保顺序）、持久化投递队列（outbox）保证 Redis 挂时也不丢实时帧。
6. 社交增强 v3：群文件、群邀请流、语音消息、内容审核 v1 已完成；群内逐条已读回执已撤回；待真实浏览器回归；搜索可升级为全文索引/ES。
7. 前端回归：群聊并入「消息」页后，浏览器真机回归好友/群/AI/文件/搜索链路，**重点覆盖本轮群文件（发文件/图片、大文件刷新恢复、成员下载）**。

## 记录模板

````md
## YYYY-MM-DD｜模块名称
```
状态： 已完成 / 部分完成 / 暂停

本次完成
- 功能或改动说明。
- 涉及的接口、数据表、配置或页面。

验证结果
- 执行的测试、构建或手动验证，以及结果。

遗留问题与风险
- 尚未处理的限制、兼容性问题或外部依赖。

下一步
- 下一次应优先处理的具体事项。
```
````

## 开发记录

## 2026-08-11｜消息列表搜索与创建入口归位

状态：已完成（前端构建与浏览器布局检查通过）

本次完成

- 恢复 `HomeView` 的 `content-header` 为页面标题与通知入口的单行布局。
- 将搜索框和“创建与添加”按钮从 `content-header` 移入 `ChatView` 的 `list-header`，位于“消息”标题下方。
- 搜索状态、结果下拉和跳转逻辑随搜索框迁移到 `ChatView`；创建菜单继续提供添加联系人、创建群聊和新建 AI 助手。
- 消息列表头部采用标题行 + 操作行结构，移动端和桌面端均保留搜索框的自适应宽度及加号按钮尺寸。

验证结果

- `npm run build` 通过：1642 个模块转换成功；仅保留既有 `config.js` 模块提示和大包体积提示。
- 当前登录页面检查通过：`.content-header` 内搜索框和创建按钮数量均为 0；`.list-header` 高度为 95px，搜索框与加号均位于其第二行。

遗留问题与风险

- 消息列表头部增加操作行，需在真实手机上确认键盘弹出时搜索下拉和会话列表的可视区域。

下一步

- 继续进行 AI Gateway 流式聊天和知识库权限增强的真实账号回归。

## 2026-08-11｜联系人滚动与群聊成员治理

状态：已完成（后端/前端构建通过，待 Flyway V21 和双账号真实回归）

本次完成

- 修复联系人页移动端底部被固定导航遮挡的问题：联系人容器保留安全区和底部滚动空间，长列表可继续滑动到最后一项。
- 消息页右上角新增统一“创建与添加”入口，集中提供添加联系人、创建群聊和新建 AI 助手，移除消息列表中不明显的创建按钮依赖。
- 群成员面板对所有成员开放“邀请好友”；开启验证时创建待处理邀请，关闭验证时直接入群并发送无操作型通知。
- 新增群成员退出群聊；群主不能直接退出，避免产生无主群。群主可在成员面板切换“加入群聊需要验证”。
- 保留群备注、成员移除、加好友等原有操作，并同步补充群聊退出和设置 REST 契约。
- 新增 Flyway V21：`chat_group.join_verification_enabled`，默认开启以兼容已有群聊的安全策略。

验证结果

- `mvn -q -DskipTests compile` 通过。
- `npm run build` 通过：1642 个模块转换成功；仅保留既有 `config.js` 模块提示和大包体积提示。
- `git diff --check` 通过（仅提示现有文件的换行符转换）。
- 当前登录会话浏览器检查通过：联系人容器 `overflow-y:auto`，底部滚动值可达到最大值；右上角“创建与添加”菜单可见并包含三个入口。

遗留问题与风险

- 本地数据库需要重启后确认 Flyway V21 执行成功；已有数据库若曾跳过 V11/V19/V20，需按迁移顺序处理后再执行 V21。
- 群主目前同时承担管理员职责，尚未实现群主转让或解散群聊；因此群主退出按钮会明确提示不能直接退出。
- 需要使用群主和普通成员两个真实账号回归：成员邀请、开启/关闭验证、接受/拒绝邀请、退出群聊以及联系人列表滚动。

下一步

- 先完成 V21 数据库迁移和双账号浏览器回归，再继续 AI Gateway 流式聊天与知识库权限增强。

## 2026-08-11｜联系人中心、群会话管理与消息置顶

状态：已完成（后端/前端构建通过，待 Flyway V19/V20 和真实账号回归）

本次完成

- 联系人页由“好友列表”调整为“联系人”，好友和群聊统一作为可保存的聊天入口；从消息列表移除后，可在联系人页重新打开并恢复会话。
- 群成员新增个人化备注、隐藏、置顶和历史清空游标；群消息清空只影响当前用户，不物理删除其他成员的聊天记录。
- 消息列表群聊增加“…”菜单，支持修改群备注、置顶/取消置顶、从消息列表移除和清空聊天记录。
- 新增群聊联系人接口：群备注、隐藏/恢复、清空历史、置顶/取消置顶；群聊新消息到达后自动恢复到消息列表。
- 新增好友/群会话置顶状态；V20 迁移后已有会话默认置顶，新创建的聊天途径默认置顶，用户可手动取消。
- 移动端更多菜单改为始终显示可点击入口，不再依赖鼠标悬停；减少手机端无法触发操作的问题。

验证结果

- `mvn -q -DskipTests compile` 通过。
- `npm run build` 通过：1642 个模块转换成功；仅保留既有 `config.js` 模块提示和大包体积提示。

遗留问题与风险

- 本地数据库尚未重启执行 V19/V20，需要确认群成员字段和会话置顶字段迁移成功。
- 群聊仍保留在联系人页，消息列表中的“移除”不等同于退出群；退出群仍使用群成员管理流程。

下一步

- 用真实账号回归好友/群聊的备注、移除、恢复、清空和置顶状态，重点验证手机端菜单。
- 视回归结果补充群聊退出、联系人分组和拖拽排序。

## 2026-08-11｜消息列表联系人化与会话删除语义

状态：已完成（后端/前端构建通过，待 Flyway V17/V18 和真实账号回归）

本次完成

- 新增 V17 会话隐藏字段 `conversation.is_archived`；“从列表移除”只隐藏当前用户视角，不删除消息，新消息到达后会自动恢复。
- 会话接口补充好友 `remark`、`displayName` 和 `contactType`，消息列表优先显示备注，并在下方保留真实昵称。
- 消息列表增加会话更多菜单：修改备注、从列表移除、清空聊天记录；清空仍为当前用户视角软删除并保留明确提示。
- 自定义 AI 助手支持本地备注，新增 V18 `ai_assistant.remark` 和备注接口；系统 AI 继续保持只读身份。
- 自定义 AI 助手增加二次确认的彻底删除入口，删除助手配置、BOT 用户、双向会话、聊天消息以及私有知识库文档/向量分片；普通好友不提供影响对方数据的物理删除。
- AI 助手继续使用独立 BOT 联系人身份，不写入普通好友关系表，但在消息列表中可以像联系人一样备注、隐藏、清空和管理。

验证结果

- `mvn -q -DskipTests compile` 通过。
- `npm run build` 通过：1642 个模块转换成功；仅保留既有 `config.js` 模块提示和大包体积提示。

遗留问题与风险

- 本地数据库尚未重启执行 V17/V18，需要确认旧会话默认 `is_archived=0` 且 AI 助手备注字段正常迁移。
- “彻底删除”针对自建 AI 助手；普通好友仍采用软删除/隐藏，避免影响对方视角。大文件资产当前不允许作为 AI 聊天附件，若后续开放需补充物理文件清理策略。

下一步

- 用真实账号回归备注、隐藏、清空、自建 AI 助手删除及私有知识库级联清理。
- 根据回归结果补充会话置顶、未读标记和群聊会话隐藏。

## 2026-08-11｜私有助手知识库

状态：已完成（前后端构建与测试通过，待 Flyway V16 和真实账号回归）

本次完成

- 新增 Flyway V16，为知识库文档增加 `owner_id`、`assistant_id` 私有归属字段；管理员公共文档保持两个字段为空。
- 新增私有资料接口：`GET/POST/DELETE /ai/assistants/{assistantId}/knowledge...`，所有接口都会校验当前用户是助手创建者。
- 私有资料复用现有文本解析、异步分块和本地向量索引流程，支持 `.txt/.md/.pdf/.docx`；删除时同步删除向量分片并使缓存失效。
- AI 检索改为“管理员公共知识库 + 当前助手私有资料”，其他用户和其他助手的私有文档不会进入上下文；公共分类选择不限制私有资料。
- 新增私有知识库页面和聊天页入口，用户可以查看索引状态、上传资料和删除资料；系统 AI 助手不显示私有资料入口。
- AI 回复来源标记支持区分“私有资料”和公共知识库来源。

验证结果

- `mvn test -q` 通过；测试环境仍输出既有 `test` 数据库账号无权限的知识库恢复日志，但退出码为 0。
- `npm run build` 通过：1642 个模块转换成功；仅保留既有 `config.js` 模块提示和大包体积提示。
- `git diff --check` 通过。

遗留问题与风险

- 本地数据库未启动，尚未真实执行 V16；需要重启后端确认 V15→V16 迁移。
- 私有资料目前使用普通 multipart 上传，受 Spring 10MB 请求限制；超大资料后续可接入 tusd 后再异步入库。
- 扫描件 OCR、图片理解和音频转写尚未接入，当前上传后会进入 FAILED 并提示无法提取文本。

下一步

- 用真实账号回归私有资料上传、索引完成、问答引用、删除和跨账号越权访问拒绝。
- 回归稳定后，再扩展用户共享知识库和文档级权限模型。

## 2026-08-11｜AI 助手标识与知识库范围优化

状态：已完成（前后端编译通过，待 Flyway V15 和真实账号回归）

本次完成

- 消息列表中的系统 AI 和用户自定义助手统一显示 `AI` 标签，AI 头像增加高亮边框；进入聊天后在标题旁显示“AI 助手”，避免与普通好友混淆。
- 新建 AI 助手的知识库选择从单个分类改为多选范围；不选择表示使用管理员开放的全部范围。
- 新增 Flyway V15：知识库文档增加 `ai_enabled`，助手增加 `knowledge_categories` JSON 范围字段；旧的单分类字段保留兼容。
- 管理员知识库上传支持选择/新建分类，并可选择“允许用户 AI 助手使用”或“仅管理员可见”；文档列表和预览显示当前 AI 范围，可单独调整。
- 用户助手只能看到管理员开放且已完成索引的分类；检索层再次过滤 `ai_enabled` 文档，避免通过接口参数绕过范围控制。

验证结果

- `mvn -q -DskipTests compile` 通过。
- `npm run build` 通过：1640 个模块转换成功；仅保留既有 `config.js` 模块提示和大包体积提示。
- 已同步更新 `api文档.md` 的多分类助手请求体与管理员知识库范围接口说明。

遗留问题与风险

- 本地数据库未启动，尚未真实执行 V15；重启后端时需确认 Flyway 从 V14 正常升级到 V15。
- 当前范围粒度是“文档分类 + AI 可用开关”；文档级用户/团队权限和独立知识库空间仍属于后续阶段。

下一步

- 重启后端完成 V15，分别用管理员和普通账号回归上传、关闭 AI 使用、助手多选范围、跨分类检索和越权访问。
- 回归稳定后再进入独立知识库空间和对话游戏状态机设计。

## 2026-08-11｜AI 流式控制与知识库来源可追溯

状态：已完成（构建与测试通过，待真实账号/模型回归）

本次完成

- 新增 `AI_STREAM_CANCEL` WebSocket 协议和“停止生成”按钮；服务端按用户与 streamId 标记取消，后续 token 不再推送，被停止的半截回答不写入消息表。
- 新增 `AI_STREAM_CANCELLED` 结束事件，前端将当前气泡收口并显示“已停止生成”，避免等待态长期挂起或刷新后出现重复回复。
- 知识库新增带来源的 `SearchHit` 检索结果，保留 chunk、文档、文件名、分类和相似度；原有 `search()` 文本接口继续兼容。
- AI RAG 上下文增加来源标识，`AI_STREAM_DONE` 返回参考来源，聊天气泡显示参考文件与知识库分类，便于用户判断回答依据。

验证结果

- `mvn test -q` 通过；测试环境仍打印既有 `test` 数据库账号无权限的知识库恢复日志，但退出码为 0。
- `npm run build` 通过：1640 个模块转换成功；仅保留既有 `config.js` 模块提示和大包体积提示。
- `git diff --check` 通过。

遗留问题与风险

- 当前取消是协作式取消：前端和服务端会立即收口，但模型供应商不支持硬取消时，远端推理可能仍继续消耗额度。
- 本地 MySQL/Redis 未启动，尚未完成真实账号、真实模型流式和浏览器多尺寸回归。

下一步

- 先完成浏览器真实回归，再进入对话游戏状态机：从一个可恢复的回合制文本游戏模板开始，接入 AI Gateway 的流式输出和知识库引用。

## 2026-08-11｜知识库无关问题误召回修复

状态：已完成（后端测试通过，待真实知识库问答回归）

本次完成

- 将 BGE 检索默认相似度阈值从 `0.3` 调整为 `0.55`，低于可靠阈值的片段不再注入 AI 上下文；仍可通过 `AI_KB_MIN_SCORE` 调整。
- 增加“词面锚点或更高语义分数（默认 `0.68`）”二阶段门槛，减少“你是谁/闲聊”等泛问题召回知识库内容；阈值可通过 `AI_KB_SEMANTIC_FALLBACK_SCORE` 调整。
- 增加同一文档的片段数量上限（默认 2，可通过 `AI_KB_MAX_CHUNKS_PER_DOCUMENT` 调整），并按文档与内容去重，避免参考来源重复刷屏。
- 保留“检索不到可靠片段即不注入知识库”的行为，无关问题回到普通对话路径。

验证结果

- `mvn test -q` 通过；测试环境仍打印既有 `test` 数据库账号无权限的知识库恢复日志，但退出码为 0。
- `git diff --check` 通过。

遗留问题与风险

- 阈值是基于当前本地 BGE 模型的保守默认值，真实资料规模、语言和问题类型变化后仍需用一组相关/无关问题校准；若召回过少，可在 `application-local.yml` 设置 `AI_KB_MIN_SCORE` 为 `0.50~0.55`、`AI_KB_SEMANTIC_FALLBACK_SCORE` 为 `0.65~0.68` 之间重新验证。

下一步

- 使用真实账号分别测试知识库相关问题、闲聊问题和跨分类问题，记录相似度分布后再决定是否加入关键词/二阶段重排。

## 2026-08-11｜新建 AI 助手页面滚动与操作体验优化

状态：已完成

本次完成

- 修复 HomeView 内容区域 `overflow: hidden` 导致 PC 端新建助手页面无法滚动的问题，页面改为独立滚动容器并补充底部安全间距。
- 修正滚动范围仅作用于新建助手页面，移除操作栏负外边距并禁止横向溢出，避免其他页面出现侧边滑动观感。
- 修复 HomeView 窄屏布局的 `100vw` 宽度溢出，主容器改为 `width: 100%` 并补充 `min-width: 0`，全局禁止横向滚动。
- 将创建流程分为“基础设置”和“回答方式”两段，助手类型保留大触控卡片，移动端改为单列布局，便于滑动浏览和选择。
- 移动端增加吸顶返回区和底部吸附操作区，滚动填写长文本时仍可快速取消或提交；PC 端提交操作也会在滚动过程中保持可见。
- 修正卡片与表单的盒模型，避免窄屏出现横向溢出。

验证结果

- `npm run build` 通过：1640 个模块转换成功。
- `git diff --check` 通过。

遗留问题与风险

- 当前本地 MySQL/Redis 未启动，尚未执行真实浏览器页面回归；后端重启后仍需验证 Flyway V14 和创建接口。

下一步

- 启动本地前后端，用 PC 和移动端分别验证页面滚动、创建、返回消息页和 AI 对话链路。

## 2026-08-11｜用户自定义 AI 助手创建与对话接入

状态：已完成（前后端构建通过，待本地数据库迁移后进行真实账号回归）

本次完成

- 新增 `V14__user_ai_assistants.sql`，为每个用户创建的助手保存名称、助手类型、角色设定、默认操作、知识库分类和专属 BOT 用户身份。
- 新增 `/ai/assistants`、`/ai/assistants/categories` 接口；自定义助手只能由创建者访问，系统默认 AI 助手仍保持全体登录用户可用。
- 消息 WebSocket 与 AI 流式回复改为按目标 BOT 用户路由，创建助手后会在消息列表生成独立会话，历史消息和清空会话也按当前助手隔离。
- 新增消息页“新建 AI 助手”入口与独立创建页面，支持类型卡片、知识库分类选择、角色设定、默认操作和快捷操作模板；创建成功后自动跳回对应 AI 对话。
- RAG 检索支持按知识库分类过滤，管理员维护的知识库可以按分类提供给用户助手使用。

验证结果

- `mvn test` 通过：5 个测试全部通过；测试环境仍会输出 `test` 数据库账号无权限的知识库恢复日志，但不影响测试结果。
- `npm run build` 通过：Vite 生产构建成功；仅保留既有 `config.js` 模块提示和大包体积提示。

遗留问题与风险

- 需要重启本地后端使 Flyway 执行 V14，并使用真实登录账号验证创建、刷新、消息发送和跨账号访问拒绝。
- 当前助手删除/编辑接口尚未开放；如后续需要管理已创建助手，再补充编辑、禁用和删除流程。

下一步

- 先完成 V14 数据库迁移后的真实账号回归，再评估助手编辑、禁用和知识库文档权限模型。

## 2026-08-11｜社交增强 v3 与移动端聊天回归完成

状态：已完成

本次完成

- 用户确认录音语音、群邀请、敏感词拦截和隐私设置均已完成真实功能测试。
- 结合前序浏览器检查，好友/群聊天中的语音、图片、视频/文件消息和多尺寸移动端布局完成回归。
- 群成员权限入口已确认：普通成员不显示群主专属邀请操作。

验证结果

- 用户反馈所有待验收功能均已测试完成。
- 自动化测试：`mvn test` 通过，5 个测试全部通过。
- 前端构建与样式检查已通过，`git diff --check` 无空白错误。

遗留问题与风险

- 知识库文档分类目前仅用于管理端组织和列表筛选，AI RAG 检索尚未按分类过滤。
- 知识库仍使用内存向量缓存，未接入文档级权限和更大规模向量存储。

下一步

- 进入知识库增强阶段，优先实现 AI 检索的分类过滤，再评估文档权限和向量库升级。

## 2026-08-11｜内容审核自动化测试补充

状态：已完成

本次完成

- 新增 `ContentModerationServiceTest`，覆盖空内容快速返回、敏感词配置为空、大小写不敏感匹配、配置词空格清理和未命中场景。
- 测试使用 Mockito 隔离 `SystemConfigMapper`，不修改本地敏感词配置和业务数据。

验证结果

- `mvn test` 通过：5 个测试全部通过（内容审核 3 个，应用上下文/文件上传 2 个）。
- 测试环境仍会输出既有 `test` 数据库账号无权限的知识库恢复日志，但不影响测试结果。
- `git diff --check` 通过。

下一步

- 继续浏览器端麦克风授权回归；群邀请需切换群主账号后再验证接受/拒绝通知。

## 2026-08-11｜浏览器回归：群成员权限入口

状态：部分完成（成员列表验证通过；邀请提交待群主账号）

本次完成

- 在已登录群聊中打开“群成员”面板，成员列表和“加好友”入口正常显示。
- 当前账号为普通群成员，未显示群主专属邀请操作，确认前端按权限隐藏邀请入口。
- 关闭成员面板后页面恢复正常，未产生通知或群成员数据变更。

验证结果

- 浏览器 DOM 检查：群成员弹窗正常打开和关闭，成员信息可读，权限入口符合当前账号身份。

遗留问题与风险

- 群邀请完整链路仍需群主账号发起、另一账号接受或拒绝，当前账号权限不足以完成提交验证。

下一步

- 切换到群主账号继续验证邀请发送，再使用被邀请账号验证通知铃铛的接受/拒绝操作。

## 2026-08-11｜浏览器回归：聊天附件与移动端聊天布局

状态：部分完成（好友会话附件与响应式布局通过；录音权限、群邀请和敏感词链路待继续验证）

本次完成

- 使用已登录浏览器进入好友会话，确认历史语音消息、文件消息和普通文字消息均能正常渲染。
- 确认语音播放器外层透明、语音卡片与外层容器宽度一致；文件卡片不再叠加文字消息背景。
- 在 360px、390px、414px 和 768px 视口下检查聊天页，页面无横向溢出，语音容器随视口响应式缩放。

验证结果

- 360px：聊天页 `scrollWidth=360`，语音卡片与外层容器均为 248px。
- 390px：聊天页无横向溢出，语音卡片与外层容器均为 278px。
- 414px：聊天页无横向溢出，语音卡片与外层容器均为 302px。
- 768px：聊天页无横向溢出，语音卡片与外层容器均为 304px。

遗留问题与风险

- 尚未点击录音按钮验证麦克风权限和真实录音；浏览器权限弹窗需要用户明确授权。
- 群邀请需要两个真实账号配合，敏感词拦截需要准备可用的敏感词配置，尚未完成端到端验证。

下一步

- 在用户授权麦克风后验证录音、上传、发送和播放；再使用两个账号回归群邀请，并验证敏感词拒绝消息不落库不广播。

## 2026-08-11｜阶段 1/阶段 2：本地浏览器回归前置

状态：部分完成（登录页窄屏回归通过；聊天业务链路待真实账号验证）

本次完成

- 启动并检查本地 Spring Boot、tusd 与 Vite 服务，确认后端 8088 和 tusd 1080 可用；Vite 使用 5173 进行临时回归。
- 在 360px、390px、414px 和 768px 视口下检查登录页，页面无横向溢出。
- 确认浏览器可以正常加载登录页，账号、密码、登录、注册和管理员登录入口均可见。

验证结果

- 浏览器 DOM 检查：四个视口的 `document.documentElement.scrollWidth` 均等于视口宽度，未发现横向溢出。
- 真实聊天、语音、群邀请、敏感词和附件链路尚未执行，因为当前浏览器没有可复用的登录会话，不能代替用户输入账号密码。

遗留问题与风险

- 需要用户在本地页面登录后，才能继续好友/群聊消息和移动端软键盘流程回归。

下一步

- 用户登录后继续验证语音播放、图片/视频/文件消息、群邀请、敏感词拦截及聊天页四种移动端宽度布局。

## 2026-08-10｜聊天附件消息气泡与语音播放器样式优化

状态：已完成（前端构建通过；待真实浏览器视觉回归）

本次完成

- 语音消息改为统一宽度的响应式播放器卡片，左右消息使用相同尺寸，避免浏览器原生播放器宽度不一致。
- 语音卡片保留麦克风图标、标题、文件大小和播放控制，并按发送方区分配色。
- 图片、视频、文件和语音消息移除外层文字气泡背景，仅保留各自内容容器样式；普通文字消息保持原有气泡样式。

验证结果

- `npm run build` 通过（1638 modules transformed）。
- `git diff --check` 通过；构建仅保留既有 `config.js` 模块属性和大分包提示。

遗留问题与风险

- 尚未在真实浏览器和移动端设备确认不同浏览器音频控件的视觉差异及触控体验。

下一步

- 启动本地前后端，回归好友/群聊中的语音播放、图片、视频/文件展示，以及群邀请、敏感词拦截和移动端聊天流程。

## 2026-08-10｜修复：Flyway V12 校验和不一致导致后端无法启动

状态：已完成

本次完成

- 恢复已执行迁移 `V12__group_read_receipt_privacy.sql` 的原始内容，避免修改已落库迁移造成 checksum mismatch。
- 保留 `V13__remove_group_read_receipt.sql` 作为正式回滚迁移，删除群已读回执遗留字段。

验证结果

- 本地数据库 Flyway 已记录 V13 成功，`chat_group_member.visible_read_message_id` 已删除。
- 备用端口 8091 启动验证通过。

下一步

- 重启 IDEA 中的后端实例，继续浏览器真机回归。

## 2026-08-10｜社交增强 v3：语音消息

状态：已完成（后端测试与前端构建通过；待浏览器真实麦克风回归）

本次完成

- 前端聊天页新增录音按钮，使用浏览器 `MediaRecorder` 录制最长 120 秒的 WebM/Opus 音频，并提供录音计时、麦克风权限提示和停止录音操作。
- 语音复用现有小文件/Tus 大文件上传链路，好友与群聊均可发送；刷新恢复任务保留 `messageType=AUDIO`。
- 消息气泡新增原生音频播放器，历史消息与会话预览均可识别语音。
- 后端 `CHAT_MESSAGE` 与 `GROUP_MESSAGE` 将 `AUDIO` 作为受控文件消息校验 `/files/**` 就绪状态，并复用现有文件字段落库和成员下载权限。
- `api文档.md` 补充 `AUDIO` 消息类型和群消息示例。

验证结果

- `mvn test` 通过（2 项）。
- `npm run build` 通过（1638 modules transformed）。
- `git diff --check` 通过。

遗留问题与风险

- 尚未使用真实浏览器麦克风验证权限、录音、好友/群双方播放；Safari 等浏览器可能使用不同 MediaRecorder 编码。

下一步

- 浏览器回归语音消息链路；随后进入社交增强 v3 的内容审核模块。

## 2026-08-10｜社交增强 v3：内容审核 v1

状态：已完成（后端测试与前端构建通过；待真实用户链路回归）

本次完成

- 新增 `ContentModerationService`，统一读取管理端 `sensitive.words` 配置并进行大小写不敏感匹配。
- 动态和评论创建时拦截命中内容；好友聊天和群聊文字通过 WebSocket 返回 `REJECTED_CONTENT`，不落库、不广播。
- 管理端敏感词说明扩展为动态、评论和聊天文字；API 文档补充拒绝状态。

验证结果

- `mvn test` 通过（2 项）。
- `npm run build` 通过（1638 modules transformed）。
- `git diff --check` 通过。

遗留问题与风险

- 当前为本地词库规则，未接入第三方/AI 审核；浏览器真实双用户消息拦截尚未回归。

下一步

- 启动本地前后端后回归语音与敏感词拦截；再评估第三方内容审核接入。

## 2026-08-10｜撤回：群内逐条已读人数

状态：已撤回

本次完成

- 因产品体验收益有限，撤回群消息已读人数、群已读回执及前端展示。
- 原有 `chat_group_member.last_read_message_id` 群未读计算继续保留；Flyway `V13` 清理历史公开游标字段。

验证结果

- `mvn test` 与 `npm run build` 已在撤回前通过；撤回后需重新验证。

遗留问题与风险

- 不再提供群已读人数和群已读回执。

下一步

- 完成群邀请的浏览器回归；随后继续语音消息或内容审核模块。

## 2026-08-10｜社交增强 v3：群邀请流

状态：部分完成（代码与构建验证通过；待真实登录用户端到端验收）

本次完成

- 新增 Flyway `V11__group_invitations.sql`，建立 `chat_group_invitation`：邀请人、被邀请人、群、状态、创建/处理时间；同一群对同一用户保留一条可重新发起的邀请记录。
- `GroupService` 新增群主邀请好友、待处理邀请列表、接受/拒绝邀请；接受后才写入 `chat_group_member`，并向双方写入/推送系统通知。
- `GroupController` 新增 `POST /groups/{id}/invitations`、`GET /groups/invitations`、`PUT /groups/invitations/{id}`。
- 群主成员管理弹窗新增显式“邀请好友加入群聊”区域，自动列出尚未入群的好友并可一键邀请；通知铃铛对 `GROUP_INVITE` 提供接受/拒绝操作。
- 已同步更新 `api文档.md` 的群邀请接口契约。

验证结果

- `mvn test` 通过（2 项）；测试环境的既有 test 数据库无权限日志不影响 Maven 成功结果。
- `npm run build` 通过（1638 modules transformed）。
- `git diff --check` 通过。

遗留问题与风险

- 尚未以两个真实登录用户验证：群主邀请 → 被邀请者通知铃铛接受/拒绝 → 群列表和成员列表实时刷新。
- 当前仅群主可邀请自己的好友；群管理员、邀请有效期、撤回及邀请备注暂未实现。

下一步

- 使用浏览器完成群邀请和群文件的真机回归；再评估语音或内容审核。

## 2026-08-07｜社交增强 v3：群文件功能（群聊文件上传/发送/下载）

状态：已完成（后端测试 2/2 + 前端构建通过 + curl/WS 端到端验证；待浏览器真机回归）

本次完成

- 群聊支持文件：群成员可在群内上传/发送文件与图片（此前 v1 群仅文字）。
- 后端：
  - `ChatUploadIntentRequest` 增加 `groupId`（与 `receiverId` 二选一）；`FileServiceImpl.createChatUploadIntent` 对群目标校验群成员，落 `purpose='GROUP'`、`receiver_id=群id`。
  - 新增 `FileService.uploadSmallFileToGroup`（群小文件 multipart，校验群成员）与 `findReadyGroupFile`（群文件就绪校验）。
  - `FileServiceImpl.findAccessibleAsset` 对 `purpose='GROUP'` 按群成员放行下载（receiver_id=群id），非成员不可读。
  - `ChatEndpoint.handleGroupMessage` 支持 `FILE`/`IMAGE`：受控 `/files/**` URL 就绪校验（不满足 ACK `REJECTED_FILE_NOT_READY` 含 reason）、解析 file 字段持久化到 `chat_group_message.file_url/file_name/file_size`、广播 `GROUP_MESSAGE` 携带文件字段。
  - `GroupService.persistMessage` 增加带 file 字段的重载。
  - `ChatController /chat/file/upload` 增加可选 `groupId` 参数（与 receiverId 二选一）。
- 前端：
  - `tusUpload.js uploadChatFileWithTus` 支持 `groupId`。
  - `ChatView.vue` 群聊显示文件上传工具栏（此前隐藏）；`handleFileUpload`/`sendUploadedFileMessage`/`queuePendingFileMessage`/`pollPendingFileMessage`/`restorePendingFileMessages` 全部支持群上下文（GROUP_MESSAGE 发送 + 刷新恢复）。
  - 新增 `sendUploadedGroupFileMessage`（群文件/图片 GROUP_MESSAGE + 乐观气泡）。
  - `handleGroupMessage` 对同 `clientMessageId` 乐观气泡合并真实 id 与 file 字段；群历史消息经 `normalizeMessage`；会话列表群文件预览复用 `getConversationPreview`。
- 无需新迁移：`chat_group_message` 已含 `message_type/file_url/file_name/file_size`，`file_asset` 已含 `receiver_id/purpose`。
- `api文档.md`：4.4 文件上传补 `groupId`；4.5 补 tusd 真实链路说明；WS 类型表补 `GROUP_MESSAGE`（发送与广播）。

验证结果

- `mvn test` 2/2 通过；`npm run build` 通过；`git diff --check` 干净。
- curl 冒烟（临时实例 8091，测试用户 grouptest1/grouptest2，已清理）：
  - 群小文件上传 200，DB 确认 `purpose=GROUP`、`receiver_id=群id`。
  - 群成员下载 200；非成员无令牌下载 404；非成员上传/创建意图被拒（"不是群成员"）。
  - 群上传意图（大文件 tus 链路）200，DB 确认 `purpose=GROUP`。
- Node WS 端到端（临时实例）：合法群文件 `FILE` 消息 ACK `SENT` 且 `chat_group_message` 落库 `file_url/file_name/file_size`；伪造 `/files/` URL ACK `REJECTED_FILE_NOT_READY`（含 reason）；图片消息 ACK `SENT`（content=图片 URL）；同 `clientMessageId` 重发幂等返回原 id。

遗留问题与风险

- 前端 UPLOADING 中刷新无法跨刷新续传 tus 字节流（既有限制，同好友文件），刷新中断会提示重新上传。
- 群文件仅走文件消息，暂未做独立「群文件」仓库列表/按文件浏览。
- 需浏览器真机回归：群聊发文件/图片 → 双方可见、可下载；大文件群发送中途刷新恢复；群文件消息在会话列表的预览。

下一步

- 浏览器真机回归群文件链路（含群聊并入消息页后的整体回归）。
- 社交增强 v3 其余项：群邀请流、语音消息、内容审核。

## 2026-08-07｜修复：消息全部渲染成对方发的（admin 门户登录 token/userId 错位）

状态：已完成（前端构建通过）

- **现象**：admin 给 pyf 发文件后刷新，admin 端自己发的消息全部显示成「对方发的」；pyf 端正常。
- **根因**：`AdminLoginView`（管理门户 `/admin/login`）只写 `token`/`tokenExpiresAt`，**不写 userId/username**。用户先主应用登录 pyf（userId=1）→ 再进 admin 门户登录 admin → `token` 被覆盖为 admin 的，但 `userId` 仍是 1 → 回主应用聊天时 `currentUserId=1`，而消息是 admin 发的（senderId=3）→ `msg.senderId === currentUserId` 恒 false → 自己发的消息渲染成对方的（且对方消息渲染成自己的，整个会话颠倒）。
- **修复**：
  - `AdminLoginView`：admin 登录也写入 `userId`/`username`（与主应用共用同一 localStorage，保持一致）。
  - `ChatView` 挂载拉 `/user/profile` 时**自愈**：若返回的 id ≠ `currentUserId`，修正 localStorage 的 userId/username 并 `location.reload()`——覆盖任意历史脏状态（包括本次 bug 已写入的）。
- 验证：`npm run build` 通过。用户需重新登录或等自愈重载一次生效。

## 2026-08-07｜修复：大文件发送中刷新卡「校验」/文件滞留

状态：已完成（前端构建通过；待真机复测）

- **现象**：发送大文件（>10M）过程中刷新页面，文件长时间停在「校验」，或后台文件滞留 `UPLOADED`。
- **根因**：`uploadChatFileWithTus` 内部「创建 intent → tus 传字节 → 发 complete」；而 `queuePendingFileMessage` 只在 **tus 全部传完之后**才把任务写入 localStorage。若刷新发生在「tus 传完/传中间」到「queue 保存」之间，任务未落 localStorage → 刷新后 `restorePendingFileMessages` 找不到 → 无人补发 complete → 文件卡在 `UPLOADED`；若字节未传完则卡 `UPLOADING`，前端轮询永远等（直到后端 TTL 清成 EXPIRED）。
- **修复**（前端）：
  - `tusUpload.js uploadChatFileWithTus` 新增 `onIntent` 回调，创建 intent 拿到 `fileId` 即通知调用方。
  - `ChatView.handleFileUpload` 在 `onIntent` 里**立即落 localStorage 任务**（deliveryStatus UPLOADING，含 fileId/文件名/大小）。刷新后 `restorePendingFileMessages` 能恢复。
  - `restorePendingFileMessages` **重建「校验中」气泡**（之前刷新后无任何提示）。
  - `pollPendingFileMessage` 新增 `UPLOADING` 分支：字节未传完无法续传 → 提示「上传中断，请重新上传」并移除任务；`UPLOADED` 分支补发 complete 恢复（已存在）。
- 清理：两个滞留测试文件（139MB JDK）已删（行 + 物理文件），`file_asset` 无 `UPLOADING/UPLOADED` 残留。

遗留问题与风险

- **运行实例**：前端改动 Vite 已热更新，无需重启后端（后端 complete 已可用）。
- `UPLOADING` 文件无法跨刷新续传（前端未实现 tus 断点恢复）；刷新中断的会提示重新上传；tusd 的指纹恢复仅对「再次选择同一文件」生效。
- 需真机复测：发送大文件中途刷新 → 刷新后出现「校验中」气泡并最终自动发消息（不再卡死）。

## 2026-08-07｜真机验收问题批量修复（7 项）

状态：已完成（前端 + 后端编译测试通过 + 关键项端到端验证）

- **1 铃铛跳转「新的朋友」不刷新**：`FriendView.applyTabQuery` 切到 requests 时补 `fetchRequests()`。
- **2 好友请求备注语义错误**：`FriendshipServiceImpl.handleRequest` 不再把验证信息（request.getRemark()）设为发送方备注、也不自动设接收方备注（均 null，显示昵称，可后续改）；前端 `handleRequest` 不再传 `remark:req.nickname`，并新增好友列表「备注」按钮（`PUT /friends/{friendId}/remark`，prompt 编辑/清除）。
- **3 AI 回复延迟无提示**：ChatView 加 `aiThinking`；发给 AI 后、首个 token 前显示「AI 正在思考…」气泡（三圆点动画）；`handleAiChunk/Done/Error` 关闭。
- **4 管理端无广播入口**：`SystemConfigView` 加「系统通知广播」卡片（标题 + 内容 → `POST /admin/notifications/broadcast`）。
- **5 文件慢网无发送提示（含小文件 <10M 与大文件）**：小文件走 `POST /chat/file/upload` 但之前未设 `fileUploading`（无任何提示）；现大小文件统一在开始时设 `fileUploading + uploadingFileName`，小文件 HTTP 上传加 `onUploadProgress` 更新进度，工具栏显示「文件名 + 进度条 + 百分比」。
- **6 在线角标位置**：好友/会话列表在线点从昵称旁移到**头像右下角**（`avatar-wrap` + `presence-dot-corner`，仅在线显示）。
- **7 群里不能互加好友**：群成员对话框对非好友成员加「加好友」按钮（`POST /friends/request`）；`openGroupMembers` 拉取好友列表判 `isFriendOfMine`。

验证结果

- `npm run build` 通过；`mvn -DskipTests compile`、`mvn test` 2/2 通过；`git diff --check` 干净。
- 备注语义：pyf 以「你好呀」向 admin 发请求 → 同意后双方备注均 null（不再是验证信息）。
- 测试数据已清理；8100 已停。

遗留问题与风险

- **运行实例**：8088 需 Stop→Run 一次加载后端备注改动（前端改动 Vite 已热更新）。
- 隐私已读单边为预期行为（每方控制自己的已读共享），未改。
- 大文件断网时 tus 上传失败会弹「上传失败」（进度条消失）；断网恢复/重试属 tus 层能力，未在本轮扩展。

## 2026-08-07｜群聊并入「消息」页（ChatView 合并，复用好友消息逻辑）

状态：已完成（前端重构 + 构建通过；待浏览器真机回归）

本次完成

- **背景**：独立 `GroupsView.vue` 的 UI/操作逻辑不佳，用户要求群聊并入「消息」页，与好友会话用同一套消息 UI/操作，仅做简单区分。
- **ChatView 合并**：
  - 引入 `currentChatType`（`'FRIEND' | 'GROUP'`）；会话列表合并好友（type:FRIEND）+ AI（type:AI）+ 群（type:GROUP），AI 置顶、其余按更新时间倒序。
  - 群条目用「群」头像（首字符圆形，颜色散列）+ 小「群」el-tag 区分；无在线点。
  - `selectConversation`/`fetchMessages`/`sendMessage`/`markCurrentConversationRead`/`trySelectConversationFromRoute` 全部按 `currentChatType` 分派（群 → /groups REST + GROUP_MESSAGE WS；好友 → 原逻辑）。
  - WS 分发器加 `GROUP_MESSAGE` → `handleGroupMessage`（按 `clientMessageId` 去重乐观气泡；当前群 `markRead`；非当前群且非自己发才 `unreadCount++`）。
  - 群聊窗口复用气泡：他人群消息显示发送者昵称小标签 + 发送者头像；群不显示已读/发送状态；群输入隐藏文件上传（v1 群仅文字）。
  - 群头部「成员」按钮（成员列表 + 群主可增删）+ 会话列表头「+ 建群」对话框（从 GroupsView 抄入）。
  - 支持 `?groupId=` 深链（搜索群消息点击跳 `/home/chat?groupId=`）。
- **移除独立群聊页**：删除 `GroupsView.vue`、`/home/groups` 路由、HomeView「群聊」菜单项与 currentRoute/pageTitle 分支。

验证结果

- `npm run build` 通过（无 GroupsView/`/home/groups` 残留引用）；`git diff --check` 干净。
- 群聊后端端点（/groups REST + GROUP_MESSAGE WS + clientMessageId 广播）均在先前阶段端到端验证过，本次为纯前端合并复用。
- 浏览器真机回归（好友/群/AI/文件/搜索链路）待用户执行。

遗留问题与风险

- **运行实例**：8088 需 Stop→Run 一次（本轮前端改动无需重启后端，但先前 ChatEndpoint clientMessageId 广播改动 + 搜索类需新 classpath 才生效）。
- 群 v1 仍仅文字（无文件/逐条已读）；独立群聊页移除，入口统一走「消息」页的群条目 + 建群按钮。

## 2026-08-07｜社交增强 v2：全文搜索（用户 + 聊天记录 + 群消息）

状态：已完成（后端 + 前端 + 端到端验证通过；待浏览器真机验收）

本次完成

- **后端**：`SearchService`（com.echo.service）——`searchUsers`（username/昵称 LIKE，排除自己/BOT，附 `isFriend`）、`searchFriendMessages`（我参与的双向消息 + 软删过滤 + content LIKE，解析对方昵称/头像）、`searchGroupMessages`（我所在群的群消息，附群名/发送者昵称）；`SearchController`（`GET /search?keyword=&limit=` 返回 `{users, friendMessages, groupMessages}`）。
- **前端**：HomeView 顶栏加搜索框（Search 图标 + 防抖 250ms），下方绝对定位下拉面板三段展示（用户/聊天记录/群消息）；点击路由——用户（好友→`/home/chat?friendId=`，非好友→`/home/friends?tab=search&keyword=`）、聊天记录→会话、群消息→`/home/groups?groupId=`。FriendView 支持 `?tab=search&keyword=` 深链（切 tab + 预填 + 搜索）。

验证结果（8098 实例）

- `mvn -DskipTests compile`、`mvn test` 2/2 通过；`npm run build` 通过。
- 造数据（pyf 发 1:1 消息含 SEARCHX1、建群发群消息含 SEARCHX2）→ 搜索：`keyword=lin` 命中 linyxhe 且 `isFriend`；`SEARCHX1` 命中 friendMessages；`SEARCHX2` 命中 groupMessages；空关键字三组空。
- 测试数据已清理（搜索测试群 + 测试消息）；8098 已停；`git diff --check` 干净。

遗留问题与风险

- **运行实例**：8088 用户 IDEA 实例需 **Stop→Run**（新类）。
- LIKE 全表扫描（小规模可接受；数据量大后可换全文索引/ES）；仅搜我参与的消息（不含他人私聊/退出的群）；不含文件内容；用户结果不含在线状态（隐私一致）。
- 浏览器真机验收（搜索框交互、结果跳转）未执行。

## 2026-08-07｜社交增强 v1：群聊 + 系统通知

状态：已完成（后端 + 前端 + 端到端验证通过；待浏览器真机验收）

本次完成

- **群聊**：`V10__social_enhancement.sql` 建 `chat_group`/`chat_group_member`/`chat_group_message`（避 MySQL 保留字 `group`）/`notification` 四表。`GroupService`（建群/我的群含最后消息与未读/成员增删/历史分页/`markRead` 置 `last_read_message_id` 游标/`isMember`/`persistMessage` 幂等 `(sender_id, client_message_id)` 唯一/`broadcastToMembers` 按成员 `publishToUser` 扇出）。`ChatEndpoint` 新 WS `GROUP_MESSAGE`（校验成员 → 幂等落库 → `MESSAGE_ACK` → 向群成员广播，跨实例 pub/sub）。`GroupController`（/groups 端点）。
- **系统通知**：`NotificationService.notify`（插行 + WS `NOTIFICATION` 帧实时推送，离线登录拉取）+ `notifyAll`（admin 全体广播）；`NotificationController`（列表/未读数/已读/全部已读）；`AdminNotificationController`（`POST /admin/notifications/broadcast`，isAdmin）。钩子：`FriendshipServiceImpl.sendRequest` → 通知接收者「新的好友请求」；`handleRequest` ACCEPT → 通知请求方「好友请求通过」。
- **前端**：新 `GroupsView.vue`（两栏：群列表含未读徽标 + 群聊窗口，建群对话框含好友多选，成员弹窗增删，WS 处理 GROUP_MESSAGE/ACK + 乐观发送）；router 加 `/home/groups`；`HomeView` 加「群聊」菜单项 + `.content` 顶栏通知铃铛（el-badge 未读数 + el-popover 通知列表，自己的 WS 过滤 NOTIFICATION，挂载/focus 轮询）；`FriendView` 支持 `?tab=requests` 深链。

验证结果（8097 单实例；V10 迁移生效）

- `mvn -DskipTests compile`、`mvn test` 2/2 通过；`npm run build` 通过。
- **群聊**：pyf 建群（加 linyxhe）→ 双方 `/groups` 可见；pyf 发 GROUP_MESSAGE → **linyxhe 实时收到**；linyxhe 未读 >0 → `PUT read` 后清零；历史分页含该消息。
- **通知**：pyf 向 admin 发好友请求 → admin 实时收到 `NOTIFICATION`（FRIEND_REQUEST）+ unread-count +1；admin 同意 → pyf 收到 ACCEPT 通知；admin 全体广播 → pyf 收到 SYSTEM 通知。
- 测试数据已清理（测试群、pyf↔admin 好友关系、测试通知）；8097 已停；`git diff --check` 干净。

遗留问题与风险

- **运行实例**：8088 用户 IDEA 实例需 **Stop→Run**（新类 + V10 DDL）。
- v1 取舍：群消息仅文字（无文件/邀请流）；群内无逐条已读（仅未读游标，隐私一致）；各视图各自 `useWebSocket`（最多同刻 2-3 连接）。
- 通知类型目前仅好友请求/被通过/SYSTEM 广播；可扩展。
- 浏览器真机验收（群聊创建/收发、通知铃铛）未执行。

## 2026-08-07｜消息可靠性 v3：隐私开关（在线/已读默认隐藏）+ 跨节点实时推送（Redis pub/sub）

状态：已完成（后端 + 前端 + 端到端验证通过；待浏览器真机验收）

本次完成

- **隐私开关（每用户，默认关闭）**：`V9__user_privacy_settings.sql` 给 `user` 加 `show_online_status` / `show_read_receipts`（默认 0）；`UserServiceImpl.updateProfile` 复制（Boolean null 守卫）；前端 SettingsView 加「隐私」页签两个 el-switch。门控：
  - **在线**：`FriendshipServiceImpl.getFriendList`、`ChatServiceImpl.getConversations` 的 `online` 字段加 `&& 对方.show_online_status`；`ChatEndpoint.broadcastUserOnline/Offline` 在用户关闭时**不广播**（`userWantsOnlineVisible`）。
  - **已读**：`handleMessageRead` 在读者关闭 `show_read_receipts` 时**不发回执、不写 is_read/read_at**（发送者见「已发送」），`markRead` 仍清读者未读徽标。
- **跨节点实时推送（Redis pub/sub）**：新建 `WsEventPublisher`（`publishToUser`/`publishBroadcast`，频道 `echo:ws:push`）、`WsEventSubscriber`（`MessageListener`，投递到本实例本地会话）、`RedisPubSubConfig`（`RedisMessageListenerContainer` 单监听线程保顺序）。`ChatEndpoint` 所有投递点改为**发布**（NEW_MESSAGE / 已读回执 / 打字 / 呼叫 / AI 流式 / 在线广播），暴露 `deliverToUser`（返回是否送达）/`deliverBroadcast`；保留本地直发（MESSAGE_ACK/心跳/拒绝/呼叫 OFFLINE）；CALL_SIGNAL 用 `PresenceService.isOnline`（Redis 共享存在）判离线。发布失败降级为本实例直发（DB 仍是离线真源）。

验证结果（8095 + 8096 双实例共享 DB/Redis）

- `mvn -DskipTests compile`、`mvn test` 2/2 通过；`npm run build` 通过。
- **隐私（默认关）**：pyf 在线但好友列表 `online:false`；`PUT show_online_status=true` 后 `online:true`；pyf 读消息（默认关回执）→ linyxhe 收不到 MESSAGE_READ_RECEIPT；开启后收到。
- **广播隐私**：pyf 关闭时 linyxhe 收不到 USER_ONLINE（隔离重跑 PASS；首跑有 PUT/连接竞态）。
- **跨实例**：pyf 连 8095、linyxhe 连 8096 → 双向互发消息均跨实例实时收到（pub/sub）。
- **单实例回归**：AI 流式（经 pub/sub）正常（1+1=2）。
- 测试后重置 pyf/linyxhe 隐私为默认 0/0；双实例已停；`git diff --check` 干净。

遗留问题与风险

- **运行实例**：8088 用户 IDEA 实例需 **Stop→Run**（新类 + V9 + pub/sub 订阅者）。
- **pub/sub fire-and-forget**：Redis 挂/订阅者重连瞬间丢实时帧 → DB 真源 + 重连补拉兜底；AI 流式丢 token 由 DONE 全量替换兜底。
- **订阅单线程**：保证 NEW_MESSAGE 顺序；高并发是单点瓶颈（可后续并发消费 + 分片）。
- 隐私默认关闭（最隐私，开启才分享）；关闭已读回执时 `is_read` 不置位（发送者视角始终「已发送」）。
- 浏览器真机验收（隐私开关、双实例体验）未执行。

## 2026-08-07｜收尾清理：孤儿 ISO 记录 + 前端 /upload/ 死代码

状态：已完成

本次完成

- **孤儿 ISO 记录**：DB 中 4 个 READY 的 ISO `file_asset` 记录，核实磁盘与消息引用后：`1fab6054`(CentOS-7)、`69f1ccce`(Rocky-8.6)、`bdde515b`(CentOS-Stream-10) **磁盘缺失且无聊天消息引用** → 已置 `EXPIRED`（受控下载将拒绝）；`ca8157fc`(CentOS-7) 磁盘有 4.7GB 文件且被消息 367 引用 → **保留**。`824892c7` 原已 EXPIRED。
- **前端 request.js /upload/ 死代码**：确认 DB 各表 `/upload/` 引用归零后，`resolveUploadUrl` 移除全部 `/upload/` 分支（相对路径/各 host 替换/URL 解析兜底），仅保留 `/files/` 受控下载路径处理；顺带修复 URL 解析兜底分支**丢失 query（access token）**的隐患（补 `parsed.search`）。

验证结果

- `npm run build` 通过；`git diff --check` 干净。
- DB 复核：4 个 ISO 中 3 个 EXPIRED、1 个 READY（真实文件）；各表 `/upload/` 引用为 0。

遗留问题与风险

- 无（纯清理；受控下载对 EXPIRED 记录将拒绝访问，可在管理端删除记录释放空间）。

## 2026-08-07｜知识库增强 v2：断点恢复 + 文档分类 + 搜索/预览

状态：已完成（后端 + 前端 + 端到端验证通过；待浏览器真机验收）

本次完成

- **断点/持久化索引恢复**：`KbIndexRecovery`（`@EventListener(ApplicationReadyEvent)`）启动时找卡在 `PENDING/INDEXING` 的文档 → `KbIndexWorker.recoverDocument(docId)` 从已存 `content` 重建索引（先删残留分片再 `indexText`，复用去重 Set + 状态机）。进程重启不丢在途索引。
- **修复 v1 潜藏 bug**：`KbService.indexText` 之前**漏写 `doc.setContent(text)`**——`content` 列从未被填充，导致预览拿不到文本、断点恢复无法重建。已补上持久化抽取文本。
- **文档分类**：`V8__knowledge_base_category.sql` 加 `category`；上传可选分类、`PUT /admin/kb/documents/{id}` 可改、管理端展示/编辑。
- **搜索/预览**：`GET /admin/kb/documents?keyword=` 按文件名/分类过滤，且列表置 `content=null` 瘦身；新增 `GET /admin/kb/documents/{id}` 返回完整文档（含 content）供预览。
- **FAILED 清理**：`KbService.markFailed` 顺带删除该文档残留分片（修复「FAILED 半成品分片仍被检索」隐患）。
- 前端：上传区加分类输入；文档列表加搜索框、分类列（行内编辑）、预览按钮 → 对话框（pre-wrap 展示 content + 元信息）。

验证结果（8094 独立验证实例）

- `mvn -DskipTests compile`、`mvn test` 2/2 通过；`npm run build` 通过。
- **断点恢复**：上传含独特事实文档（READY）→ SQL 模拟崩溃（置 INDEXING + 删分片）→ 重启 8094 → 启动恢复自动重建 → READY/1 分片 → 问 bot 答对 **RECOVER-999**。
- **分类**：上传带分类 → 列表显示；`PUT` 改分类 → 更新；按分类搜索命中。
- **搜索**：按文件名命中；不匹配关键词 → 0 条。
- **预览**：`GET /documents/{id}` 返回 content（修复前为 NULL）。
- 测试数据已清理；`git diff --check` 干净。

遗留问题与风险

- **运行实例**：8088 用户 IDEA 实例需 **Stop→Run** 加载新类（V8 是纯 DDL 无新 jar）；8094 验证实例已停。
- **分类仅管理端**：bot 检索暂不按分类过滤（用户问题不带分类）；后续可做「每用户可用分类白名单」或检索时按分类过滤。
- 恢复会重新分块嵌入（复用已存 content，启动负载略高、幂等）；扫描件 OCR 与向量库升级仍为后续。
- 浏览器真机验收（分类/搜索/预览、断点恢复）未执行。

## 2026-08-07｜消息可靠性 v2：多标签页会话路由 + 已读时间 read_at

状态：已完成（后端 + 前端 + 端到端验证通过；待浏览器真机验收）

本次完成

- **多标签页会话路由（修真实 bug）**：`ChatEndpoint.onlineUsers` 从 `Map<Long, Session>`（最新会话覆盖旧会话）改为 **`Map<Long, Set<Session>>`**（`ConcurrentHashMap.newKeySet()`）。新增 `sendJsonToUser` 扇出辅助；`onOpen` 用 `computeIfAbsent(...).add(session)` 注册；所有投递点（NEW_MESSAGE 转发、MESSAGE_READ_RECEIPT、TYPING、CALL_SIGNAL、AI `sendToUser`、USER_ONLINE/OFFLINE 广播）改为**扇出到该用户全部存活会话**。`onClose`/`onError` 从集合移除本会话，仅当集合清空（最后一个会话关闭）才标记离线 + 广播 USER_OFFLINE；删除 `hasOtherLiveSession`（语义并入集合判空）。`sessionWriteLocks`/`sendText` 已按 Session 串行化，扇出安全。
- **已读时间 read_at**：`V7__message_read_at.sql` 给 `message` 加 `read_at datetime`；`Message` pojo 加 `readAt`（MyBatis 自动映射）；`handleMessageRead` 更新时 `set("read_at", now)` 且回执 data 加 `"readAt", now.toString()`；`AiChatServiceImpl.messageToMap` 加 readAt。前端 `handleReadReceipt` 存 `m.readAt`，`.msg-status` 显示「已读 HH:mm」（复用 `formatTime`）。

验证结果（8093 独立验证实例，不打扰 8088）

- `mvn -DskipTests compile`、`mvn test` 2/2 通过；`npm run build` 通过。
- **多标签页**：两个 pyf 会话 + linyxhe 发消息 → **两个 pyf 会话都收到 NEW_MESSAGE id=447**（修复前只有最新一个）。
- **read_at**：linyxhe 收到 `MESSAGE_READ_RECEIPT {readAt:"2026-08-07T15:53:41", messageIds:[447], readerId:1}`；DB `message 447 is_read=1 read_at=2026-08-07 15:53:42`；`GET /chat/messages` 返回 `readAt`。
- **多会话存在（Set 模型）**：隔离单会话测试 linyxhe on→2、off→1（Set onClose 正确）；`git diff --check` 干净。

遗留问题与风险

- **运行实例**：V7 是纯 DDL（无新 jar），但 8088 用户 IDEA 实例需 **Stop→Run** 加载新 `Message.readAt`/ChatEndpoint 类（DevTools 可热加载类，稳妥起见完整重启）；8093 验证实例已停。
- 呼叫/打字信号扇出到目标用户全部标签页（1:1 通话由用户在任一标签页接听），行为合理。
- 已读时间 `formatTime` 只显 HH:mm，跨天只显时分（可接受）。
- 跨节点（多实例）实时推送仍为后续（Redis pub/sub）；本阶段解决单实例内多标签页。

## 2026-08-07｜修复：AI 固定文案回复不显示（需刷新才能看到）

状态：已修复（前端）

- **现象**：管理员关闭 AI 助手（`ai.enabled=false`）后，用户给 bot 发消息，要刷新页面才看到「AI 助手功能已被管理员关闭」的回复。同理影响非文字/无 key/幂等回放等**固定文案**回复。
- **根因**：这些场景后端只推 `AI_STREAM_DONE`（不推 `AI_STREAM_CHUNK`）；前端 `ChatView.handleAiDone` 依赖 `streamMessages` 里已有的流式气泡（由 `handleAiChunk` 创建），找不到就 `if (!msg) return` 直接丢弃——回复虽已持久化（刷新重拉可见），但实时 WS 显示丢失。
- **修复**：`handleAiDone` 在无流式气泡时，直接用 DONE 的 `message` 创建 bot 气泡（`{ ...finalMsg, aiStreaming:false }`），并补发 MESSAGE_READ（正打开 bot 会话时）。`ChatView.vue`。
- **验证**：`npm run build` 通过；待浏览器复现确认。

## 2026-08-07｜知识库增强 v1：PDF/Word 解析 + 异步索引

状态：已完成（后端 + 前端 + 端到端验证通过；待浏览器真机验收）

本次完成

- **依赖**：`org.apache.pdfbox:pdfbox:3.0.8`（PDF 文本提取，`Loader.loadPDF(byte[])` + `PDFTextStripper.setSortByPosition(true)`；3.x 无 `PDDocument.load(byte[])`）+ `org.apache.poi:poi-ooxml:5.5.1`（docx，`XWPFWordExtractor`）。**不引入 Tika**（重、冲突面大）。
- **异步索引**：`KbService.ingest` 拆为——`submitUpload`（同步校验扩展名/空文件 + 插 PENDING 文档）、`extractText`（按扩展名分派 txt/md/pdf/docx）、`indexText`（分块+嵌入+落库+置 READY）、`markFailed`（置 FAILED + error_message）。新增 **`KbIndexWorker`**（`@Async void indexDocument`，复刻 FileFinalizationService 模式：去重 Set + 状态机 + finally 清理），由 **KbController** 跨 bean 触发（自调用会绕过 @Async 代理）。状态流转 `PENDING→INDEXING→READY/FAILED`，上传请求立即返回不阻塞。
- **V6__knowledge_base_extensions.sql**：`kb_document` 加 `error_message varchar(1000)`（索引失败原因）；`KbDocument` pojo 加字段。
- **前端**：上传 accept 加 `.pdf/.docx`；状态列「索引中」带闪烁点 + `FAILED` 失败 tooltip（error_message）；有文档在索引时每 2s 轮询 `fetchDocuments` 到终态。

验证结果

- `mvn -DskipTests compile`、`mvn test` 2/2 通过；`npm run build` 通过。
- **异步流程**（8092 独立验证实例，不打扰 8088）：上传 PDF/DOCX 立即返回「已提交索引，正在解析」→ 2s 内 `READY`（各 1 分片）。
- **PDF 解析**：问「secret PDF token」→ bot 答 **KBPDF-7788** + 颜色 blue（PDF 由 pdfbox 生成，事实「The secret PDF token is KBPDF-7788」）。
- **DOCX 解析**：问「secret DOCX token」→ bot 答 **KBDOCX-9900** + 上海（docx 由 PowerShell zip 生成）。
- **失败路径**：空文件/不支持扩展名在提交前被拒（「文件为空」「仅支持 .txt/.md/.pdf/.docx」）；损坏 PDF → 提交成功但 `FAILED` + error_message「End-of-File...」。
- **删除**：删测试文档后 stats 归零；`git diff --check` 干净。
- PDFBox 首次运行会构建字体缓存（日志量大，属正常）。

遗留问题与风险

- **运行实例**：8088（用户 IDEA 实例）classpath 无 pdfbox/poi，需 **Stop 后重新 Run** 加载新 jar；8092 验证实例已停止。
- **异步任务重启中断**：`@Async` 无持久化队列，进程重启会中断索引中的文档（保持 PENDING/INDEXING 不自动重试）；断点/持久化索引队列留后续。
- **扫描件 PDF 无文本层**：PDFBox 只能提取数字文本层，需 OCR（未做）；多栏/表格排版阅读顺序可能错乱（`setSortByPosition` 缓解）；`.doc` 旧格式不解析。
- 浏览器真机验收（管理端上传 pdf/docx、索引状态流转）未执行。

## 2026-08-07｜AI Gateway v3：知识库 RAG（本地中文嵌入 + 余弦检索 + 管理端）

状态：已完成（后端 + 前端 + 端到端验证通过；待浏览器真机验收）

本次完成

- **依赖**：`dev.langchain4j:langchain4j:1.18.1`（聚合：DocumentSplitters 等）+ `dev.langchain4j:langchain4j-embeddings-bge-small-zh-v15:1.18.1-beta28`（本地中文嵌入，ONNX 打包，512 维，**无需 API key**）。两者均解析到既有 `langchain4j-core:1.18.1`。**不引入 easy-rag**（默认英文 + 重型 tika 依赖）、m3e/`langchain4j-onnx`（Maven Central 不存在）。
- **Flyway V5__knowledge_base.sql**：`kb_document`（文档元数据 + 原文）+ `kb_chunk`（分片文本 + embedding JSON 字符串，唯一键 document_id+chunk_index）。
- **`com.echo.service.KbService`**：`BgeSmallZhV15EmbeddingModel` 懒加载单例；`ingest`（自定义字符级分块 500/50 → 批量嵌入 → 落库，大小上限 1MB）；`search`（query 加 BGE 中文指令前缀「为这个句子生成表示以用于检索相关文章：」→ 嵌入 → 缓存向量余弦 top-k，低于 min-score 过滤）；`listDocuments`/`deleteDocument`/`stats`；向量缓存 ingest/delete 后失效。Redis 挂了不影响（与 RAG 无关）。
- **`KbController`**（`/admin/kb`，isAdmin 守卫）：`GET documents`、`POST documents`（multipart，仅 .txt/.md/.markdown/.text，UTF-8）、`DELETE documents/{id}`、`GET stats`。
- **`AiChatServiceImpl` 集成**：persona SystemMessage 之后、历史消息之前，追加一个含知识库片段（`【片段N】`）的 SystemMessage；知识库空/检索低于 min-score → 不注入，普通对话不受影响。配置 `app.ai.kb.*`（enabled/chunk-size/chunk-overlap/top-k/min-score/max-doc-size）。
- **前端**：新管理端页 `KnowledgeBaseView.vue`（状态卡：文档/分片/模型/开关 + 拖拽上传 + 文档表格删除）；路由 `/admin/kb` + AdminLayout「知识库」菜单（Collection 图标）。

验证结果

- `mvn -DskipTests compile`、`mvn test` 2/2 通过；`npm run build` 通过。
- **关键：新增 jar 后 DevTools 热重启无法加载新依赖（classpath 固定）**——`mvn` 编译测试成功但运行中 IDEA 实例报 `NoClassDefFoundError`。已结束旧实例，改由 `mvn spring-boot:run`（含新 classpath）托管 8088。
- V5 迁移生效（flyway version=5 success=1，kb_chunk/kb_document 建表）。
- **端到端**：上传含「默认端口 8088」事实的测试文档 → `已索引 1 个分片`、stats 文档数=1；问「默认后端端口是多少」→ bot 答 **8088** 且带上文档其他细节；无关问题「1+1」→ 正常答 2（不劫持）；删除文档 → stats 归零；清空 AI 会话历史后再问 → bot **不知道**（瞎猜 8080，证明 RAG 与多轮记忆各自独立且都正确）。
- `git diff --check` 干净。

遗留问题与风险

- **运行实例**：8088 现由 `mvn spring-boot:run` 托管（新增 langchain4j-embeddings jar 后 IDEA 需 **Stop 后重新 Run** 才能接管；重建 classpath 含新 jar）。
- 仅文本格式（txt/md），不解析 PDF/Word；同步嵌入（1MB ≈ 2k 分片、数秒级）；检索为内存缓存余弦（数千分片后可换 pgvector/ES/milvus）；无多文档分类/权限。
- bge-small-zh 为本地语义模型，效果对中文良好但弱于云端大模型嵌入；后续可换 OpenAI/通义 embedding（需 API key）。
- 浏览器真机验收（管理端上传/删除、AI 知识库问答效果）**已由用户执行并通过**（2026-08-07）。

## 2026-08-07｜消息可靠性 v1：Redis 在线状态 + 心跳淘汰 + 已读展示 + 重连补拉

状态：已完成（后端 + 前端 + 端到端验证通过；待浏览器真机验收）

本次完成

- **Redis 在线状态（`com.echo.service.PresenceService`）**：用 Redis ZSET `presence:online`（member=userId，score=最近心跳 epoch）做可查询的存在账本，提供 `markOnline/refresh/markOffline/isOnline/getOnlineCount/sweepStale`；Redis 不可用时全部降级（isOnline=false / count=0），不影响登录/聊天/文件。内存 `ChatEndpoint.onlineUsers` 仍是 WS 路由表（需 Session），两者在连接/关闭/心跳时同步。
- **服务端心跳超时淘汰**：`ChatEndpoint` 新增 `lastHeartbeatAt`（Session→时间）；`@Scheduled` 每 60s 清扫：关闭心跳超时（默认 120s）的死会话（触发 onClose 完整清理 + 标记离线），并清 Redis 陈旧成员。配置 `app.presence.*`（online-ttl-seconds 150 / evict-idle-seconds 120 / sweep-interval-ms 60000）。
- **多会话正确性**：onClose/onError 改为「仅当该用户无任何存活会话才标记离线并广播」——修复了多标签页下 map 覆盖导致的存在状态错误（某会话关闭不再误报用户离线；最后一个会话关闭才会离线）。
- **列表在线字段**：`GET /chat/conversations`、`GET /friends/list` 返回 `online`（`FriendshipServiceImpl` 第 213 行 `// 需要Redis状态` 占位已实现）；`GET /admin/monitor/stats` 新增 `onlineUsers`。
- **前端 webSocket.js**：服务端存活看门狗（无入站帧 >65s 主动断开触发重连）；持久重连（指数退避 3s→30s 封顶，默认不再 5 次放弃）；`close code 1003`/原因含 Authentication → 清登录态 + 触发 `auth-failed`；`connect()` 前置 token 过期检查（`request.js` 导出 `getValidToken`/`clearAuthStorage`）。
- **前端 ChatView**：会话列表在线点（后端 `online` 初始值 + `USER_ONLINE/OFFLINE` 帧实时更新）；重连补拉（WS `open` 非首次触发时重拉会话列表与当前会话消息）；自气泡已读/已发送展示（`msg.isRead ? '已读' : '已发送'`，AI 会话不显示）；`auth-failed` 跳登录。
- **前端 FriendView**：好友在线点（绿/灰）+ `useWebSocket` 监听 `USER_ONLINE/OFFLINE` 实时更新 + 挂载/窗口 focus 刷新。

验证结果

- `mvn -DskipTests compile`、`mvn test` 2/2 通过；`npm run build` 通过。后端经 DevTools 自动热重启（PID 不变），未打扰 IDEA 实例。
- **在线状态**：WS 连接 → `USER_ONLINE` 广播 + `/friends/list` 好友 `online:true` + monitor `onlineUsers` 变化；断开 → 回落。
- **多会话**：同用户两连接，关其一仍在线（count 不变）；关最后一个才离线（count 回落）——修复前存在 map 覆盖导致的幽灵在线。
- **已读回执**：A(pyf)→B(linyxhe) 发消息 → ACK(SENT) → B 收 NEW_MESSAGE → B 发 MESSAGE_READ → A 收 `MESSAGE_READ_RECEIPT {messageIds:[408], readerId:2}`。
- **心跳淘汰**：隔离实例（8091，evict=15s/sweep=5s）连静默客户端 → **15.8s 被服务端关闭（code 1008, "Heartbeat timeout"）**。
- `git diff --check` 干净（仅存量 CRLF 警告）。

遗留问题与风险

- **多节点实时推送未做**：Redis 目前只做存在账本；跨节点 NEW_MESSAGE/已读/打字推送需 Redis pub/sub 或 outbox（v2）。
- **多标签页路由**：`onlineUsers` 仍每用户只路由最新一个会话，早期标签页可能收不到推送（预先存在，未在本阶段改动）。
- 心跳淘汰阈值按后台标签页节流（~60s）保守取 120s：真死连接最多滞后 ~2 分钟才被剔除；可用 `APP_PRESENCE_*` 调。
- 已读状态未渲染时间（`read_at` 列未加）；浏览器真机验收（在线点、已读/已发送、断线重连补拉）尚未执行。

## 2026-08-06｜AI Gateway v2：多轮记忆 + Markdown 渲染 + 清空会话 + 管理端开关

状态：已完成（后端 + 前端 + 端到端验证通过；待浏览器真机验收）

本次完成

- **多轮记忆（后端）**：`AiChatServiceImpl` 不再只传 `[SystemMessage, UserMessage]`，而是从 `message` 表查询该用户与 bot 最近 `app.ai.context-window-messages`（默认 20，环境变量 `AI_CONTEXT_WINDOW_MESSAGES`）条 TEXT 消息，按时间正序转成 `UserMessage`/`AiMessage` 拼进 `model.chat()` 的消息列表。**设计决策：DB 上下文窗口而非 `MessageWindowChatMemory`**——AI 对话本身就在 `message` 表，天然持久化（重启不丢）、与清空会话自动联动（软删即失忆）、单一数据源，且无需新增 `dev.langchain4j:langchain4j` 聚合模块依赖。上下文查询按发送/接收方向分别过滤 `deleted_by_sender`/`deleted_by_receiver`。
- **管理端 AI 开关**：`system_config.ai.enabled`（缺省启用，`"false"` 时关闭）。后端 `AiChatServiceImpl` 关时回固定「AI 助手功能已被管理员关闭，请稍后再试。」；`GET /ai/bot-info` 新增 `enabled` 字段。前端 `SystemConfigView.vue` 顶部新增「AI 助手」el-card + el-switch（读写 `/admin/system/configs`，失败回滚）。
- **清空会话**：把 `ChatServiceImpl.deleteMessages` 的 `"ALL"` 存根实现掉——软删当前用户视角的消息（我发的置 `deleted_by_sender`，对方发的置 `deleted_by_receiver`），并新增 `ConversationService.clearConversation` 重置本方向会话行（unread=0、last_message_id=null）；`ChatServiceImpl.getMessages` 补软删过滤（否则清空后历史仍返回）；新增 `DELETE /chat/conversations/{friendId}`。AI 与普通好友通用。前端 `ChatView.vue` 在 AI 会话头部加清空按钮（`ElMessageBox.confirm` → DELETE → 清空本地气泡），并用 `ignoredStreams` Set 忽略清空瞬间在途流的残余 CHUNK/DONE，防气泡复活。
- **Markdown 渲染（仅 bot 气泡）**：前端新增依赖 `marked@^12` + `dompurify@^3`。`ChatView.vue` 里 `renderMarkdown`（`DOMPurify.sanitize(marked.parse(content))`）仅对 `senderId === aiBotId` 的 TEXT 气泡 `v-html`，其余保持纯文本；流式 CHUNK 更新 content 自动重渲染。补 `.markdown` 的 `:deep()` 样式（p/pre/code/a/ul/ol/table/blockquote/headings/img），处理父级 `word-break: break-all` 对代码块/空白的影响。

验证结果

- `mvn -DskipTests compile`、`mvn test` 2/2 通过（BUILD SUCCESS）；`npm run build` 通过（dist 含 ChatView 208 kB，新增 marked/dompurify）。
- 后端已重启（8088，PID 31216；旧的 mvn 实例被 TaskStop 后 JVM 仍在，`taskkill //PID 26552` 后重启成功）。
- **多轮记忆**：pyf 对 bot 发「我叫小明…」→ bot 记住；再问「我刚才说自己叫什么名字？」→ bot 答「**小明**」。
- **清空**：`DELETE /chat/conversations/10` 返回「已清空」；清空前历史 17 条 → 清空后 `/chat/messages?friendId=10` 返回 `messages: []`；再问「我叫什么名字？」→ bot 答「还不知道你的名字」——记忆与清空联动生效。
- **管理端开关**：admin(uid=3) `PUT /admin/system/configs {ai.enabled:false}` → `GET /ai/bot-info` `enabled:false`，pyf 发消息收到固定「已关闭」回复；恢复 `true` → `enabled:true`。
- `git diff --check` 无空白错误（仅存量 CRLF 警告）。

遗留问题与风险

- **运行实例**：当前 8088 由 `mvn spring-boot:run` 启动。IDEA 正常开发需 **Stop 后重新 Run**（新增依赖后 classpath 需重建），届时先停掉当前 mvn 实例。
- 清空瞬间在途流无法服务端取消（该版 `StreamingChatModel` 无 cancel 句柄），靠前端忽略残余事件；极端时序下 bot 回复可能先落库再被清空软删覆盖，可接受。
- 多轮上下文按消息条数计（≈10 轮），未做 token 级窗口；普通好友会话清空按钮与 AI 会话物理删除未做，为后续项。
- 浏览器真机验收（Markdown 渲染视觉效果、清空按钮、管理端开关、多轮记忆）尚未执行。

## 2026-08-06｜AI Gateway：LangChain4j + DeepSeek 流式聊天

状态：已完成（v1：单轮文字、WS 流式、回复持久化、优雅降级）

本次完成

- 依赖：`langchain4j-open-ai-spring-boot-starter` **1.18.1-beta28**（langchain4j 的 Spring Boot starter 只有 `-betaXX` 版号，核心模块 langchain4j-open-ai 1.18.1 为稳定版；BOM 版号与 starter 不对齐，故在依赖上直接钉版本）。配置文件：`application.yml` 加 `app.ai.*` 与 `langchain4j.open-ai.streaming-chat-model.*`（base-url `https://api.deepseek.com/v1`，langchain4j 不自动拼 /v1）；DeepSeek API key 写入 `application-local.yml`（gitignored）。
- **环境修复**：全局 Maven settings 的 aliyun 镜像为 HTTP（`http://maven.aliyun.com/...`），被 Maven 3.8.1+ HTTP 阻止器拦截，新依赖无法下载。新增用户级 `~/.m2/settings.xml` 用 HTTPS 镜像（`https://maven.aliyun.com/repository/central/`）覆盖，不改全局文件。
- Flyway **V4__ai_bot.sql**：`user.role` 枚举加 `'BOT'`；插入固定 username `ai_assistant` 的 bot 用户。**不写死 id**：实测 InnoDB 不允许把 AUTO_INCREMENT 重置到低于当前 max(id)，显式高 id 会让下一个真实用户拿到巨大 id；改为普通自增 + 按 username 解析。
- 后端：
  - `com.echo.ai.BotUserService`：按 username 懒解析并缓存 bot 身份（id/昵称/头像）。
  - `com.echo.ai.AiChatService(Impl)`：幂等（`client_message_id = "ai:<用户消息id>"`）、非 TEXT 兜底文案、无 key/无模型兜底文案；`StreamingChatModel` 流式回调把 token 推 `AI_STREAM_CHUNK`，完成时持久化 bot 回复并推 `AI_STREAM_DONE`，异常推 `AI_STREAM_ERROR`。1.x API：`StreamingChatModel` / `StreamingChatResponseHandler` / `onPartialResponse` / `onCompleteResponse`（旧 0.x 教程名已过时）。
  - `ChatEndpoint`：新增公开静态 `sendToUser`；`handleChatMessage` 对 bot 目标跳过好友校验，ACK 后异步触发 AI；**抽取 `ConversationService`** 共用会话更新逻辑。
  - `ChatServiceImpl.getConversations` 过滤 bot 会话；新增 `GET /ai/bot-info`（前端据此注入合成会话）。
  - **并发修复**：WS 处理线程（写 ACK）与 AI 流式回调线程（写 CHUNK）同时写同一 session 触发 `TEXT_FULL_WRITING`。给每个 Session 一把写锁，全部 12 处 `sendText` 走串行化 helper（`sessionWriteLocks`），onClose 清理。
- 前端：`ChatView.vue` 挂载时拉 `/ai/bot-info`，会话列表顶部注入合成「AI 助手」条目；WS 分发器加 `AI_STREAM_CHUNK/DONE/ERROR` 三分支，`streamMessages` Map 以 streamId 关联临时气泡，DONE 用持久化消息回填；TEXT 分支加流式光标与错误提示；与 bot 聊天时隐藏音视频按钮。

验证结果

- `mvn test` 2/2 通过；`mvn -DskipTests compile`、`npm run build` 通过。
- V4 迁移生效（flyway_schema_history version=4 success=1）；bot 用户 `ai_assistant` role=BOT status=1；`GET /ai/bot-info` 返回 `{botUserId:10, botNickname:"AI 助手"}`。
- 端到端（Node WS 客户端 + 真实 DeepSeek）：发「你好」→ ACK(SENT) → 流式 CHUNK → DONE(持久化 id=369)；历史经 `/chat/messages?friendId=10` 完整返回。
- 并发：两用户同时问 bot，各自独立 ACK→流式→DONE，无 `TEXT_FULL_WRITING`。
- 幂等：同 `clientMessageId` 重发 → 返回既有 serverMessageId，不重复生成（DB 无重复 bot 回复）。
- 非文字：给 bot 发 FILE → 固定「我目前只能回复文字消息哦～」。
- 会话列表过滤：`/chat/conversations` 不含 bot id；前端注入合成条目无重复。

遗留问题与风险

- **运行实例**：当前 8088 由 `mvn spring-boot:run` 启动（IDEA 启动的旧实例 classpath 不含 langchain4j，新增依赖后 DevTools 无法热加载新 jar，已结束该进程）。正常开发需在 IDEA **Stop 后重新 Run**（会重建含 langchain4j 的 classpath），届时先停止当前 mvn 实例。
- v1 单轮对话、无多轮记忆、bot 回复不渲染 Markdown；均为后续项。
- bot 会话 unread 恒 0（合成条目）；用户中途离开视图则回复仍持久化，重开可见。
- 测试期间产生的 bot 对话留在 DB（pyf 等用户的 AI 历史），属正常数据。
- 工具 `tools/ai-ws-test/`（Node WS 客户端，含 `node_modules/ws`）仅本地验证用。

下一步

- IDEA 重启后端进入正常开发流程；浏览器真机验收 AI 助手（含断网/刷新）。
- 多轮记忆（ChatMemory/MessageWindow）、Markdown 渲染、AI 会话清空/删除、管理端 AI 开关（system_config）。

## 2026-08-06｜阶段 2：关闭旧公开目录（阶段 C）

状态：已完成

本次完成

- 删除 `WebMvcConfig.java`（其唯一职责是 `/upload/**` 静态映射）；同步移除 `application.yml` 中已死的 `app.upload-dir` 配置（全库已无 `/upload/**` 引用，不再需要公开目录）。
- `SecurityConfig` 的 `/upload/**` permitAll 移除，旧目录不再有匿名访问豁免。
- 清理 `upload/` 根目录 20 个孤儿文件（DB 零引用），迁入 `upload/backup-20260806/orphan-files/` 保留可恢复性；`upload/tusd`、`upload/files`、`upload/backup-20260806` 目录保留。
- 删除依赖旧目录的测试 `uploadDirFileIsPubliclyAccessible`，移除因此不再使用的 `TestRestTemplate`/`LocalServerPort`/`ResponseEntity`（保留 `RANDOM_PORT`，因 WebSocket `serverEndpointExporter` 需要 Servlet 容器）。
- `ChatEndpoint` 更新过时注释（不再保留 `/upload/**` 兼容链路，文件消息 URL 一律走受控 `/files/**`）。

验证结果

- `mvn test` 2/2 通过（contextLoads + uploadSmallFileCreatesControlledAsset）。
- 已认证 `GET /upload/__probe__` → **404**（映射已移除）；未认证 → **403**（permitAll 已移除）；`GET /files/{id}/content?access=...` → **200**（受控下载完好）。
- 全库扫描 `%/upload/%`（user.avatar_url / post.media_urls / message.file_url / message.content / file_asset）：引用归零。
- `git diff --check` 无错误。

遗留问题与风险

- 前端 `echo-frontend/src/util/request.js` 仍有 `/upload/` 透传死代码（URL 不会被生成，无害；可选清理）。
- 3 个迁移前的孤儿 ISO 记录（READY 但磁盘无文件、无聊天消息）仍存在，可后续改为 `EXPIRED` 或删除。
- 真机大文件断网/刷新/取消/过期回归仍未执行。

下一步

- 真机执行 9GB 级断网/刷新/取消/过期回归（需浏览器），验收后推进消息可靠性与 AI Gateway（LangChain4j）。

## 2026-08-06｜阶段 2：小文件迁移到受控文件服务（A+B）

状态：已完成

本次完成

- 新增 `FileService.uploadSmallFile(ownerId, file, receiverId)`：multipart 小文件同步建 `file_asset`、写 `upload/files/{id}`、SHA-256，返回 `/files/{id}/content?access=...` 受控 URL；`/chat/file/upload`（ChatServiceImpl.uploadFile）改为委托调用，响应结构不变，前端零改动。头像/动态图/聊天小文件三处前端调用点无需修改。
- 新增一次性迁移 `LegacyFileMigrationRunner`（`--app.migrate-legacy-files=true` 触发，幂等）：把 DB 中 35 处 `/upload/**` 引用（user.avatar_url 6 / post.media_urls 7 / message.file_url 6 / message.content IMAGE 16）改写为受控 URL，物理文件从 `upload/` 搬入 `upload/files/{id}`，同一文件按文件名去重复用 asset。
- 更新测试：`uploadFileSavesToUploadDir` → `uploadSmallFileCreatesControlledAsset`（`FileAssetMapper` 用 `@MockitoBean`，测试不连真实库）。

验证结果

- `mvn test` 3/3 通过；`npm run build` 通过。
- 迁移执行：35 引用改写、35 物理文件迁移，4 处 `/upload/**` 引用全部归零；迁移后头像（30KB）、消息图片（642KB）、消息文件（311KB）下载均 `200`。
- 新 `/chat/file/upload` 实测返回 `/files/{id}/content?access=...`（status `READY`）且可下载；8088 后端经 DevTools 热重启已加载新代码。
- 迁移前已备份受影响表至 `upload/backup-20260806/pre-migration.sql`。

遗留问题与风险

- 公开 `/upload/**` 静态目录与 `permitAll` 仍保留（阶段 C 再做：关闭旧目录）。`upload/` 下仍有约 20 个未被 DB 引用的孤儿文件未清理。
- 3 个迁移前的孤儿 ISO 记录（READY 但磁盘无文件、无聊天消息）仍存在，可后续改为 `EXPIRED` 或删除。
- 小文件与新文件共用 `file_asset`，AVATAR/POST 用 access token 直链访问；公开静态目录关闭前，两者可共存。

下一步

- 阶段 C：移除 `/upload/**` 公开静态映射与 `permitAll`，关闭旧目录，清理孤儿文件；随后真机大文件回归，再进入消息可靠性与 AI Gateway。

## 2026-08-06 — 阶段 2：tusd 存储路径解析修复与端到端验证

状态：已完成

本次完成

- 定位并修复存储路径解析错误：IDEA 的 Spring Boot 运行配置未指定工作目录，模块根为项目根，后端实际以**项目根**为 `user.dir`；而 `application.yml` 使用 `../upload/tusd` 与 `../upload/files`，从项目根解析到了项目外的 `D:\AA_LocalCode\upload\...`，导致受控下载 `/files/{id}/content` 全部 404、完成确认读写错位。
- 修正为与 `app.upload-dir: upload`、tusd（`start-tusd.ps1` 的绝对路径 `upload\tusd`）及前端 `dev.mjs` 一致的 `upload/tusd`、`upload/files`；更新 `CLAUDE_HANDOFF.md` 纠正「工作目录为 echo-backend」的错误前提。

验证结果

- Flyway V1/V2/V3 均成功应用；`message.client_message_id` 唯一索引存在；无卡在 `PROCESSING`/`FAILED` 的任务。
- 受控下载恢复正常：43MB wav 全量 `200`、711MB zip Range `206`、旧 `/upload/**` 静态资源仍 `200`。
- 端到端真实上传（12MB）：意图 → tusd `201` 建上传 → 两段 `PATCH`（`204`，`HEAD` 续传 offset 正确）→ `complete` → `PROCESSING` → 约 2s `READY` → 下载字节数与 SHA-256 完全一致；测试数据已清理。
- 后端已由 IDEA 从修复后的 `target/classes` 重启（PID 32960），tusd（18040）正常。

遗留问题与风险

- 真正的 9GB 文件刷新恢复、断网/取消/过期完整真机回归仍未执行。
- DB 中 3 个大 ISO 文件（CentOS/Rocky，READY）磁盘文件缺失且无聊天消息，判定为旧配置期间写入项目外的历史残留，未删除；如需清理可将其状态改为 `EXPIRED` 或删除记录。

下一步

- 真机执行大文件断网/刷新/取消回归；随后迁移旧 `/upload/**` 小文件到受控文件服务。

## 2026-08-06 — 阶段 2：大文件异步恢复与可靠发送修复

状态：已完成

本次完成

- 修复 IDEA 启动目录与 tusd 写入目录不一致的问题：后端统一读取项目根目录的 `upload/tusd`，完成后写入项目根目录的 `upload/files`。
- 刷新恢复任务始终采用持久化的接收人 ID，不再依赖尚未加载完成的当前会话。
- 待发送文件消息收到 WebSocket `MESSAGE_ACK` 后才从本地恢复队列移除；新增 `client_message_id` 与唯一索引，刷新重试不会重复插入消息。
- `PROCESSING` 文件在状态查询和定时任务中都会重新投递后台校验，避免本地重启后永久轮询。

验证结果

- `mvn test` 通过（3 tests）。
- `npm run build` 通过（1634 modules）。
- 2026-08-06 晚再次重启后端（Flyway 成功应用 V3），状态查询与定时任务均已接管；9GB 文件 `bdde515b` 已转 `READY`（SHA-256 已算），下载与上传链路验证通过（详见上方记录）。

遗留问题与下一步

- 真机断网/刷新/取消/过期完整回归待执行。
- 已创建 `CLAUDE_HANDOFF.md`，记录本阶段实现、启动配置、验证状态与后续验收步骤，供切换开发工具后继续。

## 2026-08-06｜阶段 2：大文件异步确认与可恢复发送

状态：部分完成

本次完成
- `/files/{id}/complete` 改为快速将文件转为 `PROCESSING` 并投递后台任务；SHA-256、磁盘迁移与最终 `READY` 状态不再阻塞浏览器请求线程。
- 新增文件状态查询接口；聊天页在 Tus 达到 100% 后显示“文件上传完成，正在校验”，后台状态变为 `READY` 后自动发送聊天消息。
- 待处理文件任务持久化到浏览器本地存储；刷新或网络短暂中断后会恢复轮询，不再因刷新直接丢失待发送文件。

验证结果
- `mvn test` 通过，3 项测试全部成功；`npm run build` 通过，共转换 1634 个模块。

遗留问题与风险
- 尚未使用 4GB/10GB 文件完成真实耗时、刷新恢复和后台任务失败回归；当前文件消息去重仍将在阶段 3 的客户端幂等改造中进一步强化。

下一步
- 用真实大文件确认处理阶段不阻塞输入、刷新后自动恢复并在文件就绪后发送消息。

## 2026-08-06｜阶段 2：文件上限单一配置源

状态：已完成

本次完成
- `npm run dev` 启动器与 `start-tusd.ps1` 在没有环境变量覆盖时，均自动读取后端 `application.yml` 的 `app.file.max-size`。
- `start-dev.ps1` 改为让后端和 tusd 继承可选的 `APP_FILE_MAX_SIZE`；未设置时两者共同使用 `application.yml` 默认值。

验证结果
- Node 与两份 PowerShell 启动脚本语法解析均通过。

下一步
- 修改 `application.yml` 后重启 Spring Boot 与 `npm run dev`，验证 10GB 上传创建与续传。

## 2026-08-06｜阶段 2：npm 开发启动整合 tusd

状态：已完成

本次完成
- 新增 `echo-frontend/scripts/dev.mjs`，`npm run dev` 会检查 1080 端口，未运行 tusd 时自动启动 tusd，再启动 Vite；端口已有服务时复用现有 tusd。
- `npm run dev:vite` 保留为仅启动 Vite 的备用命令，便于单独排查前端。

验证结果
- `node --check scripts/dev.mjs` 与 `npm run build` 均通过。

下一步
- 在 IDEA 的 npm Run Configuration 中执行 `npm run dev`，使用真实大文件回归上传、恢复与完成确认。

## 2026-08-06｜阶段 2：本机统一文件上限环境变量

状态：已完成

本次完成
- `scripts/start-dev.ps1` 默认设置 `APP_FILE_MAX_SIZE=21474836480`，并将同一值传给 Spring Boot 与 tusd。
- `scripts/start-tusd.ps1` 的默认值同步为 20GiB；两份 PowerShell 启动脚本均完成语法解析验证。

下一步
- 重新选择 10GB 文件执行上传、刷新恢复和完成确认回归。

## 2026-08-06｜阶段 2：Tus 大文件上限联调修复

状态：已完成

本次完成
- 修复 `start-tusd.ps1` 在 Windows PowerShell 下因非 ASCII 提示文本产生的解析问题。
- 将本机运行的 tusd 重启为 `21474836480` 字节（20GiB）上限；同时将大文件完成确认请求超时提升为 30 分钟，以覆盖后端 SHA-256 校验耗时。

验证结果
- tusd `OPTIONS /files/` 响应的 `Tus-Max-Size` 为 `21474836480`；启动日志显示 `20480.00MB`。
- `npm run build` 通过，共转换 1634 个模块。

下一步
- 重新选择文件并执行 10GB 上传回归，重点检查中断恢复和完成确认耗时。

## 2026-08-06｜阶段 2：上传大小配置收敛

状态：已完成

本次完成
- 移除前端写死的 2GB 校验，前端改用后端上传意图返回的上限。
- `application.yml`、后端上传意图和 `start-tusd.ps1` 统一支持 `APP_FILE_MAX_SIZE`；启动脚本也支持 `-MaxFileSize` 参数。

验证结果
- `npm run build` 通过，共转换 1634 个模块。

下一步
- 使用同一个 `APP_FILE_MAX_SIZE` 值重启后端与 tusd 后进行真实大文件续传回归。

## 2026-08-06｜阶段 2：本机 Tus 大文件上传（第一实施切片）

状态：部分完成

本次完成
- 新增 `file_asset` 元数据表及 Flyway `V2` 迁移。后端提供聊天上传意图、完成确认、取消和受控下载接口；上传状态仅在磁盘大小与 SHA-256 二次校验通过后变为 `READY`。
- 新增 `tusd` 的 `pre-create`、`pre-finish`、`post-finish` 钩子处理：上传令牌、文件大小、接收者和过期时间均由后端校验；聊天 WebSocket 拒绝状态未就绪的 `/files/...` 文件消息。
- 新增 Uppy Tus 客户端。聊天页面保持 10MB 以内走原 `/chat/file/upload`，超过 10MB 至 2GB 自动改走 5MB Tus 分片上传并显示进度。
- 新增本机 `scripts/start-tusd.ps1` 和 `echo-backend/TUSD_LOCAL_DEVELOPMENT.md`；完成确认后的文件移动到非公开 `upload/files`，tusd 以 `-disable-download` 运行。
- 新增每小时过期任务清理策略，删除过期未确认任务的 tusd 临时文件并标记为 `EXPIRED`。

验证结果
- `mvn test` 通过，3 项测试全部成功；`npm run build` 通过，共转换 1634 个模块。
- 本机 Spring Boot 已真实执行 Flyway `V2` 迁移；tusd 已启动并确认 `pre-create` 钩子能以 `400` 拒绝无效令牌。
- tusd 的 Vite 来源 CORS 预检返回 `200`，且 tusd 文件直读返回 `405`，符合禁用公开直读的预期。

遗留问题与风险
- 尚未在真机完成中途断网、刷新后恢复、取消和完成幂等的端到端验证。
- 历史 `/upload/**` 小文件仍用于兼容现有头像、动态和聊天数据；下一切片须将其逐步迁移为受控文件服务后，才能移除公开静态目录。
- 当前本机仅做文件大小、磁盘与哈希校验；生产环境接入前需要替换为真实病毒扫描服务。

下一步
- 使用真实登录用户完成一个超过 10MB 文件的创建、暂停/刷新恢复、完成确认、消息发送和接收端下载回归；随后迁移小文件路径并关闭旧公开目录。

## 2026-08-06｜阶段 1：移动端底部导航视觉优化

状态：已完成

本次完成
- 移动端底部导航由纯深灰底色升级为深蓝渐变，增加轻量顶部分隔线与阴影，强化与页面内容的层次区分。
- 当前选中入口使用蓝紫渐变、柔和阴影和状态圆点；非选中入口采用低对比度浅蓝灰，悬停时提供克制的反馈。

验证结果
- `npm run build` 通过，共转换 1507 个模块。

遗留问题与风险
- 配色已完成代码级验证，最终观感仍建议在实际 OLED 与 LCD 手机屏幕上确认。

下一步
- 完成真机回归后关闭阶段 1，并开始阶段 2 的本机 Tus 上传接口设计。

## 2026-08-06｜阶段 1：移动端截图问题修复

状态：已完成

本次完成
- 图片消息不再继承文字消息的内边距和绿色背景；图片本身作为消息内容展示，移动端按视口宽度限制最大尺寸，消除图片外层大面积气泡空白。
- 移动端底部导航移除退出入口，仅保留消息、好友、动态和设置四项；在设置页“安全设置”中增加“账户操作 / 退出登录”按钮。桌面侧栏仍保留退出入口。
- 好友列表移动端操作区由固定宽度的纵向按钮改为可收缩横向布局，按钮与内容区均可随可用宽度伸缩，避免挤压昵称和列表卡片。

验证结果
- `npm run build` 通过，共转换 1507 个模块。

遗留问题与风险
- 图片、导航和好友操作布局已按截图问题修复；仍建议在实际手机上复核长昵称、超宽图片与系统软键盘场景。

下一步
- 完成真机回归后关闭阶段 1，并开始阶段 2 的本机 Tus 上传接口设计。

## 2026-08-06｜阶段 1：移动端前端优化（第一实施切片）

状态：部分完成

本次完成
- 新增统一的 `useMobileViewport` 组合式工具，替代聊天、好友和注册页中分散的 `matchMedia` 监听逻辑。
- 主框架在移动端改为安全区适配的 64px 底部导航，补齐触控尺寸与键盘可访问性；内容区使用动态视口高度，避免底部导航遮挡页面。
- 聊天页完善移动端单栏状态同步、输入区安全区、44px 通话操作触控区、图片/文件卡片宽度和消息区布局。
- 登录、注册、好友、动态和设置页新增单列表单、弹窗宽度、横向标签、图片网格与操作按钮的窄屏适配。
- 管理端新增横向导航、窄屏统计卡片和可横向滚动的用户、举报、系统配置表格，避免压缩关键列。

验证结果
- `npm run build` 通过，共转换 1507 个模块；管理端适配后再次构建通过。
- `git diff --check` 通过；移动端状态判断已统一至 `useMobileViewport`。

遗留问题与风险
- 受浏览器隔离环境限制，无法连接到本机 Vite 端口，因此尚未在 360px、390px、414px 和 768px 的真实浏览器/真机完成登录、聊天、加好友、发动态和修改资料全流程回归。

下一步
- 在实际手机或可访问本机端口的浏览器执行移动端交互回归；通过后将阶段 1 标记为完成，并开始阶段 2 的本机 Tus 上传设计。

## 2026-08-06｜阶段 0：工程基线与本地开发配置

状态：已完成

本次完成
- 后端从 Spring Boot 3.0.2 升级至 3.5.14，保持 Java 17；现有 Spring Security、MyBatis-Plus、Druid、Redis、邮件和 WebSocket 依赖可正常加载。
- 移除仓库配置中的数据库密码、邮箱账号/授权码和 JWT 密钥；增加被 Git 忽略的 `application-local.yml` 约定、可提交的配置模板和测试专用配置。
- 引入 Flyway 与 MySQL 支持，新增不可变的 `V1__baseline_schema.sql`；已有数据库以版本 1 接管，新空库执行基线建表。
- 新增 [后端本地开发说明](echo-backend/LOCAL_DEVELOPMENT.md)，并将旧 `echo_chat.sql` 标记为不可再用于迁移的历史导出文件。

验证结果
- `mvn test`：3 项测试通过，Spring Boot 3.5.14 测试上下文正常启动。
- `npm run build`：前端 Vite 构建通过。

遗留问题与风险
- 已使用本机 `application-local.yml` 启动后端：MySQL 与 Redis 均可达，Flyway 已验证 2 条迁移记录，schema 当前版本为 1，应用成功启动在 8088 端口；验证进程随后已停止。
- 前端构建仍有既有的 `config.js` 模块属性和大于 500 kB 分包警告，未影响构建，本阶段未处理。

下一步
- 阶段 1：开始移动端高频流程优化，优先统一视口状态并改造主框架与聊天页。

## 2026-08-06｜开发环境范围确认：仅本地运行

状态：已完成

本次完成
- 确认当前项目只在本地开发和运行，不进行阿里云部署，不接入 OSS 或其他云端对象存储。
- 大文件传输继续采用 Tus 技术选型，但本期使用本机 `tusd` 与本地磁盘完成上传、续传和受控下载联调。
- 将云存储和生产部署从当前实施切片移出，后续如有部署需求再单独进行技术选型与配置。

验证结果
- 已同步调整开发流程和下一步计划，当前阶段不依赖任何云资源账号或凭证。

遗留问题与风险
- 本地磁盘存储只适用于开发和单机运行，未来部署到多实例或公网环境时需重新引入对象存储与访问控制方案。

下一步
- 按本地环境开始阶段 0 的 Spring Boot 3.5.x 升级与回归验证。

## 2026-08-06｜技术选型确认：LangChain4j 与 Tus

状态：已完成

本次完成
- AI Gateway 确定使用 LangChain4j；通过独立模型适配层支持按机器人配置切换第三方 AI API，前端不直接调用模型服务。
- 大文件传输确定使用开源 Tus 协议：Vue 端使用 `@uppy/core + @uppy/tus`，部署 `tusd` 作为独立上传服务，Spring Boot 负责鉴权、业务规则、完成确认和文件元数据。
- 确认现有 Spring Boot 3.0.2 不满足 LangChain4j 当前 Spring Boot 集成的 3.5+ 要求；将升级到 Spring Boot 3.5.x 当前稳定补丁版本列为阶段 0 的首项工作。

验证结果
- 已核对 LangChain4j 官方 Spring Boot 版本要求，以及 Tus 协议、Uppy Tus 客户端和 tusd 的官方实现说明。

遗留问题与风险
- Spring Boot 升级时需逐项验证 Spring Security、MyBatis-Plus、Druid、Redis 和邮件依赖兼容性。
- `tusd` 的本机启动方式、上传完成回调鉴权与本地文件清理策略将在阶段 2 的接口设计中确定；云存储适配留待未来部署阶段。

下一步
- 开始阶段 0 的 Spring Boot 3.5.x 升级与回归验证；升级通过后再加入 LangChain4j 依赖。

## 2026-08-06｜开发范围调整：音视频完成与移动端优化纳入重点

状态：已完成

本次完成
- 根据已完成状态，将音视频通话生产化从后续开发与优化范围中移除，并在进度概览标记为已完成。
- 新增阶段 1“重点移动端前端优化”，覆盖主框架、聊天、好友、动态、设置、认证和管理端窄屏降级；明确统一视口状态、软键盘处理、触控尺寸和多尺寸验收要求。
- 调整后续阶段编号与依赖关系，详细路线见 [新模块开发流程.md](新模块开发流程.md)。

验证结果
- 已核对现有移动端适配仅分散于 `HomeView.vue`、`ChatView.vue`、`FriendView.vue` 和注册页，新增阶段覆盖当前未系统适配的高频页面。

遗留问题与风险
- 不同 Android/iOS 浏览器的软键盘和安全区行为需在实施时以真机复核。

下一步
- 仍先执行阶段 0；通过基线验收后，优先开始阶段 1 的移动端前端优化。

## 2026-08-06｜新模块开发阶段划分与正式启动

状态：已完成

本次完成
- 根据当前 Vue 前端页面、Spring Boot 控制器/服务、MySQL 基线脚本、WebSocket 单机在线状态和本地文件上传实现，完成新模块的阶段划分。
- 建立从阶段 0（工程基线与安全配置）到阶段 7（发布、监控与回归）的依赖顺序、工作范围和验收标准，详见 [新模块开发流程.md](新模块开发流程.md)。
- 确定第一个实施切片：先完成配置与迁移基线，再完成本地存储版大文件分片续传，验收后直接推进消息可靠性改造。

验证结果
- 已核对后端现有 `application.yml`、`echo_chat.sql`、`ChatController`、`ChatEndpoint` 和前端上传/实时通信调用位置；阶段划分与现有结构一致。

遗留问题与风险
- 云资源、模型供应商和向量存储的具体选型与凭证仍待实施阶段确认；音视频通话生产化已完成，不再纳入本开发路线。
- 数据库迁移工具的选型会影响阶段 0 的具体文件结构，但不影响本阶段划分。

下一步
- 开始阶段 0：先盘点并迁移敏感配置，再确定数据库迁移工具并建立可重复验证的开发基线。

## 2026-08-06｜新模块开发规划与日志机制初始化

```
状态：已完成
本次完成
- 建立本开发日志，作为后续新模块开发的交接依据。
- 梳理新模块的优先级：生产基础设施 → 大文件断点续传 → 消息可靠性与多节点 → AI Gateway → 知识库与社交增强功能。
- 确认当前大文件分片接口、AI 模块与数据表尚未落地；现有上传为单次 `multipart` 上传，本地 `upload/` 目录保存文件。

验证结果
- 已核对新模块规划文档与项目当前文件结构。

遗留问题与风险
- 当前本地 Redis 与 AI 模型服务的具体连接和凭证尚未配置；阿里云与 OSS 不属于当前开发范围，音视频通话生产化已完成，不再纳入后续优化范围。
- 生产部署方案需要在实际部署环境确定后再补充参数与安全策略。

下一步
- 开始第一期基础模块前，先将本次要实现的范围和验收标准补充到一条新的“进行中”记录中。
```
